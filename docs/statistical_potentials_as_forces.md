# 把统计势注入力：这不是新想法，而且我们的失败有名字

> 2026-09-10。为队列项 3（"如何把统计势注入力"）定位。
> 结论先行：**这件事文献里做过，其中一件就是我们在用的那套模型的作者做的。**
> 而 `docs/NOTES.md` 里 TriRNASP 的失败，在文献里有明确的原因和解法。

## 1. 文献

| 文献 | 做了什么 | 对我们 |
| :-- | :-- | :-- |
| **IsRNA / IsRNA1 / IsRNA2**（PMC9731381） | CG 力场的参数化方法叫 **iterative simulated reference state approach**。`E_total = E_bond(b) + E_angle(θ) + E_torsion(φ) + E_bp(r,θ,φ) + E_pair(r)`，简谐 + 高斯 + LJ 型，参数由统计方法确定 | **我们的 CG 力场本身就是统计参数化的**（同一血统）|
| **cgRNASP**（NAR Genom Bioinform 2023;5(1):lqad016, doi:10.1093/nargab/lqad016, PMC9985339） | 在**三个 CG 层级**上做的统计势：① **3 珠 = P / C4\' / N9(嘌呤) 或 N1(嘧啶)** —— 代表作；② cgRNASP-PC，2 珠 = P / C4\'；③ cgRNASP-C，1 珠 = C4\'。短程/长程按残基间距分开。与全原子 rsRNASP 性能相当，RNA-Puzzles 上略好，"strikingly more efficient" | **①的珠子定义与我们的 P/C4\'/N 是同一约定** —— 分辨率错配这一条被它填掉了 |
| **cgRNASP-CN**（Commun. Theor. Phys. 2022, doi:10.1088/1572-9494/ac7042；GitHub `Tan-group/cgRNASP-CN`） | 最小 CG 表示上的统计势 | 同上 |
| **IsRNA2+**（JCTC, doi:10.1021/acs.jctc.6c01116） | 标题即方法："Developing Explicit Base Stacking Potentials for the IsRNA2+ Coarse-Grained RNA Force Field **Using Iterative Reweighting**" —— 给现成 CG 力场加显式项 | 这就是"注入"本身 |
| **综述** Building RNA coarse-grained force fields: Design principles and training strategies（Biophys J 2026, doi:10.1016/j.bpj.2026.03.041） | CG 力场的设计考量、训练策略；序列依赖、二级结构、三级模体等结构信息如何并入；ML 方向的展望 | 该先读的入门 |
| **rsRNASP**（Biophys J 2022, PMC8758408） | 全原子统计势 | 我们那份元验证计划的对象 |
| Bernauer et al.，**可微**统计势（全原子 + CG 两个层级） | 从 cgRNASP 的引用里看到 | 【未核实原文】要"进力"就得有解析梯度 |
| SimRNA / iFoldRNA / NAST / Vfold / HiRe-RNA / oxRNA / RACER | 统计势 + MC / 离散 MD / CG MD 的范例群 | 见下条引文 |

## 2. 四条要点

### 2.1 "注入"这个说法要改

我们的 CG 力场（IsRNAcirc 血统）本来就是统计参数化的。所以不是"把外来的统计势注射进物理力场"，
而是"**在一个统计势场上再加一层统计项**"。这不是措辞问题 —— 它决定会不会**双重计数**：
知识型势本质上是自由能（PMF）的一类，里面已经隐含着物理相互作用。

### 2.2 失败的原因是分辨率错配，文献里有原话

cgRNASP 原文：

> 现有的传统统计势（RAPDF、KB、DFIRE-RNA 等）**全都基于全原子表示**。因此，**至今各个不同
> CG 层级上仍然急需可靠的 CG 统计势**。

同文另一句（点名了整条路线）：

> 几乎所有现有的物理模型都基于不同层级的 CG 表示，而不是全原子 —— 包括 SimRNA、iFold、NAST、
> IsRNA、Vfold、HiRe-RNA、oxRNA、RACER 和我们带盐效应的 CG 模型。

所以 TriRNASP 失败的病根不是"统计势不能进力场"，而是**拿全原子的统计去配 CG 的场**。
我们记的"量级差 ~50 倍、极小值不一致"正是这个错配的症状。

### 2.3 解法有两条，我们上次两条都没走

| 路线 | 代表 | 做法 |
| :-- | :-- | :-- |
| **同层级计数** | cgRNASP / cgRNASP-CN | 直接在 CG 表示上统计，不改力场 |
| **迭代再加项** | IsRNA2+（iterative reweighting）；IsRNA（iterative simulated reference state）| 反复调整势，直到模拟出的统计量复现目标统计量 |

我们上次是**一次性硬塞一个固定系数**（`trirnasp_scale = 0.002`）。
那既不是同层级计数，也不是迭代标定 —— 所以受挫是预期的。

### 2.4 进力就需要梯度

打分函数只要能量值；进力场要解析梯度。可微的统计势是有的（Bernauer et al.），
但我们没有核过原文，也没有确认它在 CG 层级上的形式是否可直接用。

## 3. 重写后的三个问题

1. **在哪一级计数？—— 开关已经查清：我们的 3 珠是名义上的，不是真的。**

   cgRNASP 的 3 珠是 **P / C4' / N9(嘌呤) 或 N1(嘧啶)**，都是真实重原子。我们的不是：

   | 位置 | 珠子怎么来的 |
   |---|---|
   | `torch_gpu_refine.py:132-133` | `pos_c4 = pos_p + bb_dir*0.034`；`pos_n = pos_p + bb_dir*(-0.015)` —— **沿骨架方向的偏移** |
   | `openmm_gpu_refiner.py:224-225` | `p + rng.normal(0, 0.3, 3)` —— **随机扰动**，注释写 “corrected later by minimize” |
   | `openmm_gpu_refiner.py:233-235` | 索引定义 `P(i)=3i, C4(i)=3i+1, N(i)=3i+2` |
   | `aform_from_template.py:52-53` | 全原子重构用的是**另一组**三锚点：**P / C1' / C4'** |

   两条后果：

   - **cgRNASP 的统计不能直接搬。** 珠子数一样（3）、名字一样（P/C4'/N），
     但 N 不是糖苷氮，C4' 也不一定是真 C4'。同名不同物。
   - 更要紧的一条：`torch_gpu_refine.py` 里 C4' 和 N **都与 P–P 方向共线**，
     所以这两颗珠子只编码局部主链切向，不携带任何垂直于主链的信息，**也就没有序列信息**。
     在它们上面加统计势，等于给“P 坐标 + 切向”再加一层统计 —— 增量可能很小。

   **出路**：`aform_from_template.py` 的 1EHZ 重构本来就会给出每个残基的**真实**全原子几何。
   从那里读出真正的 C4' 与 N9/N1，就得到一个货真价实的 CG 表示，可以在上面按 cgRNASP 的方式统计。
   **这件事必须先做**，否则后面所有关于统计势的讨论都是在装饰珠子上做文章。
2. **怎么加才不打架？** 走迭代（IsRNA2+ / IsRNA 路线），而不是固定系数。剩下两件没人替我们做：
   把 cgRNASP 的统计转成**可微**形式（它原本是打分用的距离依赖势，不是力），以及定标。
3. **加成，还是重参数化？** 底层场已经是统计的，所以"再加一层"和"重新拟合"是两条不同的路。

## 4. 证据空缺（诚实标注）

- **IsRNA2+ 正文未读**：ACS 付费，EuropePMC 未收录（按标题检索无结果）。只拿到标题里的方法名。
- ~~cgRNASP 只到摘要级~~ → 正文已读，见第 3b 节。
- **2026 综述正文未读**：只拿到摘要。
- **Bernauer 可微统计势未核**：仅从 cgRNASP 的引用列表中看到。

### 3b. cgRNASP 的实际计数方案（2026-09-10 补读正文）

原文（NAR Genom Bioinform 2023;5(1):lqad016，PMC9985339）：

- **距离 bin 宽 0.3 Å**（不是 1 Å）。
- **残基间距 k₀ = 5** 为界：长程 k ≥ 5，短程 k ∈ {1}、{2}、{3–4} 分档处理。
- 各 k 档的**距离截断值不同**，按该档内 CG 珠子的实际距离分布定。
- 长程项带一个**与链长 N 有关的归一化 f(N)**，权重 ω = ω₀ / f(N)。
- **参考态用 finite-ideal-gas**，不是 rsRNASP 的 random-walk-chain（原文说前者表现更好且被广泛使用）。
- 未见到的 bin 直接把势设成一个定值。

**训练集（关键）**

- **191 条**非冗余 RNA 天然结构，取自 **RNA 3D Hub 非冗余集（Release 3.102）**，
  与 rsRNASP 用的是同一套；数据集在 GitHub `Tan-group/rsRNASP`。
- 训练用 decoy：**35 条单链 RNA**，经 **FARFAR2 / RNAComposer / SimRNA / 3dRNA v2.0**
  四个预测器生成，二级结构既有天然也有 RNAfold / 2dRNA 预测的。
  数据集在 GitHub `Tan-group/cgRNASP`。
- 我先前脚本里写的"6409 个 RNA 单体"是**错的** —— 那是 3dRNA 模版库的数字（CSBJ 2020
  那篇），与 cgRNASP 无关。

**性能（原文自报，PCC）**

| 方法 | PCC |
|--:|--:|
| rsRNASP（全原子） | 0.73 |
| **cgRNASP** | **0.71** |
| DFIRE-RNA | ~0.70 |
| RNA3DCNN | 0.61 |
| ARES | 0.60 |
| 3dRNAscore | 0.55 |
| RASP | 0.53 |

### 3c. 可行性计数的结论

在一棵真实的三珠表示上（1EHZ，76 残基），我按 1 Å bin 数了 14,633 个珠子对距离：

| 通道组 | 距离数 | 通道 | 格子 | 占用 | 中位数 |
|--:|--:|--:|--:|--:|--:|
| 骨架–骨架 | 11,400 | 3 | 270 | 25.9% | 55 |
| 骨架–碱基 | 11,400 | 8 | 720 | 9.3% | 62 |
| **碱基–碱基** | 2,850 | 10 | 900 | **7.3%** | 12 |

只取短程档（cgRNASP 说它比 rsRNASP 更细致的那一块）：

| 通道组 | 占用 | 中位数 |
|--:|--:|--:|
| 骨架–骨架 | 21.1% | 9 |
| 骨架–碱基 | 7.5% | 4 |
| **碱基–碱基** | **5.3%** | **1** |

**携带序列信息的那一组，93% 的格子是空的，中位数 1 个计数。**

而且这已经是**宽松的一侧** —— cgRNASP 用 0.3 Å bin，格子还要多三倍。

两个结论：
1. **计数不够。** 一条 76 残基的 tRNA 撑不起同层级的势。
2. **多样性也不够，而且这条更硬。** 一条结构只给一个折叠的统计。拿相似结构去堆计数，
   喂的是参考态，不是势。cgRNASP 用 191 条非冗余结构，出处是公开的。

**所以第 1 步的答案是：能做，但必须先取数据。** 数据在
GitHub `Tan-group/cgRNASP` 与 `Tan-group/rsRNASP`，191 条天然结构 + 35 条 RNA 的 decoy 集。
（数据已于同日取回并重数，见第 3d 节。）
### 3d. 数据到手后的实测：按 cgRNASP 的官方方案重数（2026-09-10）

数据已取：D:\torusfold-cgdata\（cgRNASP 321 MB / rsRNASP 365 MB，**不在任何 git 仓库里**）。
rsRNASP\Training_set\ 里就是那 **191 条**非冗余天然结构。

**官方方案（读参考实现 cgRNASP/cgRNASP/cgRNASP.c 得到，不是从论文猜的）**

| 来源 | 内容 |
|---|---|
| k1=0, k2=1, k3=2, k4=4 | 分离档界：1 / 2 / 3–4 / ≥5 |
| Rc1=5.0, Rc2=9.0, Rc3=13.0, Rc4=24.0 | **每档各自的距离截断** |
| intervals = 17 / 30 / 43 / 80 | bin 宽恒为 0.3 Å，超出截断的距离**直接跳过** |
| data/12atom_type.dat | **12 个珠子类型**：{A,U,C,G} × {P, C4', N9 或 N1} |
| 排除规则 | 同一残基（同链且同残基号）的珠子对不计；**异链一律算长程** |
| 能量合成 | 1.0*E1 + 1.5*E2 + 2.4*E3 + 6.5*E4/fun(N)，fun(N) = -355/sqrt(N+16) + 72 |
| 势表 | 四档各自一个 .potential 文件，列格式：type_i type_j bin energy |
| 冲突处理 | 3 Å 内的对设为 30 kBT（README）；未见到的 bin 有固定填充值 |

**在这个训练集上数出来的占用（scripts/cg_occupancy_reference_scheme.py）**

```
structures   191 / 191        residues       12,299
counts       2,699,612        cell grid      24,480
non-empty    15,554 (63.5%)

 class  sep   bins   cells    used   occup   median
     1  1-1     17    2448     618   25.2%        0
     2  2-2     30    4320    2077   48.1%        0
     3  3-4     43    6192    3305   53.4%        1
     4  >=5     80   11520    9554   82.9%      198
```

非空格子里 **29.2% 的计数不到 10**；计数中位数 46、均值 174、最大 1806。

### 3e. 读法

1. **数据门槛是实测出来的，不是估的**：191 条结构 / 12,299 残基 / 270 万计数 → 63.5% 占用。
   这就是「能用」的成本。
2. **短程是结构性稀疏，不是偶然。** 类 1 只有 2,448 个格子，而要填它的是分离恰为 1 的珠子对 ——
   无论多少条结构，这个数都只是残基数的常数倍。**更长的链不解决它，更不一样的折叠才解决。**
   这正是计划书 §1b 那条「长度 × 相似度双层分层」要挡的东西。
3. **长程档反而宽裕**（82.9%），因为它是 O(N²) 累积的。
4. **对我们的直接含义**：cgRNASP 的珠子是**按碱基分型的 12 类**（连 P 和 C4' 都带碱基标签），
   而我们力场里的 C4' / N 是 p + bb_dir*offset 造出来的、**不带碱基类型、且与主链共线**。两边对不上。
   **出路是现成的**：aform_from_template.reconstruct_all_atom 的输出里本来就有模板给的
   真实 C4' 与 N9/N1。把力场的珠子定义换成它们，cgRNASP 这套就**直接兼容** ——
   不需要重新发明计数方案。
   （已实施，见第 3f 节。）
### 3f. 力场的珠子已换成真实几何（2026-09-10）

新增 `aform_from_template.real_cg_beads(p_coords, sequence, pairs=None) -> (L, 3, 3)`，
按残基返回 (P, C4', N9 或 N1)，来自 1EHZ 模板重构。缺序列或非 ACGU 时**抛错，不回退** ——
回退会悄悄把造出来的珠子还给调用方。

改了**两处**（C4' 与 N 此前都是造出来的）：

| 位置 | 原做法 | 现在 |
|---|---|---|
| `torch_gpu_refine.py:132-133` | `pos_c4 = pos_p + bb_dir*0.034`；`pos_n = pos_p + bb_dir*(-0.015)` | `real_cg_beads()` |
| `torch_cgsim.py:1880-1882` | `p_nm[i] + rng.normal(0, 0.03, 3)` | `real_cg_beads()` |
| `openmm_gpu_refiner.py:224-225` | `p + rng.normal(0, 0.3, 3)` | **未改（遗留）** |

**效果**（同一条 P 轨迹）：

| | \|P-C4'\| | \|C4'-N\| | P→C4' 与主链夹角 |
|---|---|---|---|
| 真珠子 | 3.885 ± 0.069 | 3.359 ± 0.036 | **48.9°** |
| 旧偏移 | 3.400 | **4.900** | **0.0°** |
| 力场约束目标 | 3.90 | 3.35 | — |

旧造法给出的 \|C4'-N\| 是 **4.90 Å，而力场自己的约束目标是 3.35** —— 初始化就违反了自己的键长 46%。
顺带核实：力场的珠子内目标本身是准的（`BOND_P_C4=0.390`、`BOND_C4_N=0.335` 对 1EHZ 实测
3.887 ± 0.081 / 3.428 ± 0.266，97.4% 落进 0.3 Å）。所以此前错的只是初始化。

**验证**：`BatchedREMD2D` 在 24 残基上构造并跑 5 步通过；`diag['tri_force_injected'] = False`、
`diag['tri_scores'] = None` —— 再次确认当前没有任何统计势在作用。

**遗留两条**

1. `openmm_gpu_refiner.py:224-225` 那条 CPU 路径没改。
2. 重构出来的 N 珠子相对晶体真实 N9/N1 仍差 **2.010 ± 2.328 Å**（`scripts/test_real_cg_beads.py`）——
   它继承的是重构本身的约 2 Å 摆放误差。**造势用实验结构（191 条），不受影响；
   给我们的模型打分时会受影响**，短程 bin 尤其。这一条要记着。
### 3g. 现在能注入了吗——不能，卡在梯度（2026-09-10）

把已备好的与还缺的列清：

| 需要的东西 | 状态 |
|---|---|
| 真珠子 (P, C4', N9 或 N1) | ✅ `real_cg_beads()`，已接入两条 torch 路径 |
| cgRNASP 的完整方案 | ✅ k / Rc / intervals / 12 类型 / 能量合成，全部已知 |
| 训练数据 | ✅ 191 条天然结构在 D:\torusfold-cgdata\ |
| **力场认不认珠子类型** | ❌ `cg_energy_forces` 用的是裸索引 `P(i)/C4(i)/N(i)`，**没有碱基类型**，读不了 12×12 的表 |
| 我们自己的势表 | ❌ 还没数 |
| 读表求值的代码 | ❌ 没有 |
| **梯度** | ❌ **根本问题，见下** |

**梯度为什么是根本问题**

C 实现是查表：`potential[i][j][(int)(distance/0.3)]`。表本身是**段内恒定**的：

| 文件 | bins | 相邻 bin 完全相等的比例 |
|---|---|---|
| 0-1（分离=1） | 17 | **79.0%** |
| 1-2（分离=2） | 30 | 52.9% |
| 2-4（分离=3–4） | 43 | 47.4% |
| long（分离≥5） | 80 | 15.6% |

也就是说：**最短程那一档、79% 的区间上势能是常数** ⇒ -dE/dr 恒为 0；其余的导数集中在 bin 边界，
是个 δ。直接拿它当力，积分器在 79% 的地方不受力、在边界上受到无限大的力。

当年 TriRNASP 那次必然出问题：**没有插值，直接对阶跃函数做 CPU 前向差分**。
步长小于 bin 宽得到 0，大于得到粗斜率 —— 那 38% 的方向错误就是这么来的。

**三条出路**

1. **把表插值**（线性或样条）→ 得到分段线性势，导数良定义。改动小，但会轻微改变势本身。
   这是最便宜的一条。
2. **不查表**：直接用核密度估计重算 E = -kBT·ln[P_obs(r)/P_ref(r)]，梯度可解析。
   即所谓「可微统计势」路线（Bernauer et al.，仍未核原文）。更贵，但干净。
3. **只当打分函数**（领域惯例，也是 NOTES.md 当年收敛到的结论）。

**结论**：珠子这一步已经通了；**要注入，下一步必须先造一个可微表示** ——
已做，见第 3h 节。
### 3h. 可微表示：做出来了，而且验证通过（2026-09-10）

`scripts/test_cg_potential_differentiability.py`。同一组珠子上，两种表示对照：

| | 能量 (kBT) | 解析梯度 vs 中心差分 (h = 1e-4 Å) |
|---|---|---|
| binned（C 代码原样：`int(r/0.3)` 查表） | -79.2155 | **36/36 个分量精确为 0**，max \|FD\| = 0 |
| **线性插值（bin 中心之间）** | -78.6411 | **36/36 通过**，最差 \|FD − 解析\| = **9.4e-10** |

能量差 **+0.57 kBT（0.7%）**。也就是说：**把表线性插值，就得到一个自洽的可微表示，
而势本身只变不到 1%。**

实现要点（两处我一开始写错了，记下来）：

1. **超出每档截断的距离必须跳过，不能外推。** C 代码是 `if((int)(r/0.3) < intervals)`，
   越过就丢。我第一版在表外插值，插值版的能量差了一个数量级。
2. **`1/fun(N)` 要同时作用在能量和梯度上。** 只除能量不除力，自洽性检验必然不过。
3. 两端取平（斜率 0），否则第一/最后一格会外推。

**这一步之后，注入的障碍清单变成：**

| 需要的东西 | 状态 |
|---|---|
| 真珠子 (P, C4', N9/N1) | ✅ |
| 可微表示 | ✅ 本节，已验证 |
| cgRNASP 全部参数 | ✅ |
| 训练数据 | ✅ |
| **知道 12 种珠子类型的力场代码路径** | ❌ 还没有 |
| 我们自己的势表 | ❌ 还没数（现在可以直接用他们的表做测试） |
| **定标与双重计数** | ❌ 概念问题，见下 |

**双重计数这一条仍然悬着。** cgRNASP 的势是 -kBT·ln(P_obs/P_ref)，本质是自由能；
而我们的 CG 力场本身也是统计参数化的。再加一层统计项，加的是已经隐含在里面的东西。
这跟可微性无关，是**加什么**的问题，不是**怎么加**的问题。


### 3i. 注入之后的松弛实验：方向对，但结论不能用（补测）

`scripts/relax_with_and_without_cgrnasp.py`。起点是 `real_cg_beads` 从 1EHZ 晶体 P 轨迹重建的珠子，
用阻尼梯度下降走 400 步（中位步长 0.005 Å，单珠位移上限 0.05 Å），指标是 P-superpose 后的珠子 RMSD。

| 组合 | all | P | C4' | N | E_cgrnasp (kBT) |
|---|---|---|---|---|---|
| 起点（我们的重建） | 1.241 | 0.480 | 1.230 | 2.014 | — |
| CG only | 1.977 | 1.892 | 1.255 | 2.785 | −39.96 |
| CG + 0.1 cgRNASP | 1.918 | 1.812 | 1.224 | 2.718 | −188.45 |
| CG + 0.5 cgRNASP | 1.713 | 1.456 | 1.194 | 2.490 | −674.19 |
| CG + 2.0 cgRNASP | 1.480 | 1.073 | 1.172 | 2.196 | −854.43 |
| cgRNASP only | **1.193** | 0.463 | 1.153 | 1.963 | −792.37 |

（早先一版没传 cell list，clash 项被静默跳过；那版里珠子转碱基朝向能量一模一样，作废。这里全程传了。）

**打乱对照。** 能说明"改善来自信息量"的唯一对照是把序列信息毁掉：几何、配对表、表值、插值器、
步长标定全部不动，只随机置换"哪个残基拿到哪种碱基类型"。平滑性与力的量级分布因此都保留。

| 组合 | real | null 均值 ± sd (n=12) | p |
|---|---|---|---|
| cgRNASP only | 1.193 | 1.215 ± 0.010 | 0.077 |
| CG + 2.0 cgRNASP | 1.480 | 1.530 ± 0.019 | 0.077 |

**p = 0.077。** 12 个零信息对照没有一个比真表更接近晶体，但均值只差 0.022 Å。
按纪律，p > 0.05 不能写"提供了增益"。总改善是 1.241 → 1.193，共 0.048 Å，其中 0.026 Å
来自零信息对照 —— **这份改善里一大半是势的笼统形状（平滑、压缩），不是它记住的距离依赖。**

**更要紧的是，这个实验测的不是精度。** 起点的 P 直接取自晶体 P 轨迹，P 偏差 0.480 Å
已经贴着重建自身的上界 0.444 Å（§9）——**起点的 P 本来就是对的**。于是：

- CG only 把一条误差 0.48 Å 的 P 轨迹推到 1.892 Å；
- cgRNASP only 把它留在 0.463 Å。

这读作"谁的力更温柔"，不是"谁更准"。起点已经正确，任何移动都让 P 变差，
所以这个指标天然偏向"什么都不做"的势。

**起点处的中位力：CG 200.0 kJ/mol/nm（= 20 kJ/mol/Å），cgRNASP 166.8 kJ/mol/Å。**
（早先这里写"CG 2,000.0 kJ/mol/Å，差 12 倍"，是我自己的单位错误：`cg_energy_forces` 返回
kJ/mol/nm，1 nm = 10 Å，换算应当**除以** 10，我乘了。正确的数是 CG **小** 8 倍。
真正的问题不是大小，而是 200.0 **正好是上限值本身** —— 见 §3j。）

**训练集关系（必须说明）。** 1EHZ **不在** cgRNASP 的 191 个训练结构里（该目录 192 个文件，
无 1ehz）。但 `1FIR.pdb` 在里面，61 nt，也是 tRNA —— 与 1EHZ 同一个折叠。
所以这不是自包含，是**折叠熟悉**：势见过这个折叠。§1b 那条相似度分层正是为挡住这个而设的，
这里没做，结果偏乐观。

**结论：**
1. 松弛实验不能用来判断 cgRNASP 项是否有用（p = 0.077，且指标偏向不动）。
2. 目前唯一有信息量的证据仍是诱饵排序（native 排第一，7/7），但那是单结构 n=7。
3. **我们自己的 CG 力场在近天然几何上把结构推坏** —— 这比"要不要注入"更该先解决。

**要定这件事需要的是**：多个结构、与 cgRNASP 训练集不同源（或显式分为"折叠熟悉/折叠新颖"两层）、
留一法、诱饵排序，加打乱对照。不是再调松弛步长。


### 3j. 力为什么这么大：两个目标常数不符合天然 RNA 几何，加上一个过大的前因子（补测）

**上半部分：上限在冒充力律。**
`scripts/diagnose_cg_force_terms.py` 在 §3i 的起点几何上逐项拆力（用库自己的 helper，
不是重写；13 项能量和 45,365.09 kJ/mol 对 `cg_energy_forces` 的 45,441.82，差 76.7 正是
GB/SA+Manning，所以分解可信）：

| | mean | p95 | max | 珠子数 > 200 |
|---|---|---|---|---|
| 13 项之和（未加帽） | 622.22 | 1724.67 | 2287.83 | 126 / 228 |
| `cg_energy_forces`（加帽） | 116.79 | 200.00 | 200.00 | **126 / 228 精确等于 200** |
| `cg_forces_explicit_batched`（**生产路径，无帽**） | 1885.20 | 9358.76 | **26086.69** | 86 / 228 |

单位 kJ/mol/nm。**228 个珠子里 126 个精确坐在 200 的帽子上** —— 那个"安全网"不是偶尔触发，
它就是这一路径的实际力律。而生产路径 `cg_forces_explicit_batched`（1074 行 return）
**完全没有帽**，它的力到 26,086.69 kJ/mol/nm ≈ 2,609 kJ/mol/Å。

顺带一个独立缺陷：同一个结构上，`cg_forces_explicit_batched` 的能量是 53,711.73，
`cg_energy_forces` 是 45,441.82 —— **两条库路径相差 18%**。1160 行的注释写的是
"consistent with cg_energy_forces"。它们不一致。

**下半部分：大项是哪些，以及它们为什么大。**
四项扛下了几乎全部的量：dihedral（mean 301.44，72 珠超 200）、bpp（285.04，55）、
angle（144.17，37）、stacking（104.88，71）。这四项都是"往某个目标值拉"。所以查目标值。

`scripts/check_ff_targets_vs_native.py` 对 1EHZ，`scripts/measure_targets_on_training_set.py`
对 rsRNASP 训练集里 191 个实验结构（**直接从沉积坐标取 P 原子，不经过我们任何代码**）：

| 坐标 | 力场目标 | 191 结构实测 | 逐结构 sd | 5–95% | 常数 |
|---|---|---|---|---|---|
| P-P 骨架键 (nm) | 0.590 | 0.599 | 0.025 | 0.576 – 0.654 | K_BB 500 |
| P-C4' (nm) | 0.390 | 0.388 | — | — | K_INTRA 400 |
| C4'-N (nm) | 0.335 | 0.336 | — | — | K_INTRA 400 |
| **P(i)-P(i+2) (nm)** | **0.505** | **1.112** | 0.052 | 1.058 – 1.226 | K_STACK 500 |
| P(i)-P(i+3) (nm) | — | 1.541 | 0.086 | 1.443 – 1.707 | — |
| N-N 配对 (nm) | 1.000 | **0.954** | 0.115 | 0.054 | 0.891 – 1.025 | K_PAIR 600 |
| P-P 配对 (nm) | 1.000 | 1.653 | 0.293 | 0.173 | 1.199 – 1.788 | 导引 500 |
| P-P-P cos | −0.866 (150°) | −0.743 (138°) | 0.081 | −0.873 – −0.633 | K_ANGLE 600 |
| **P-P-P-P cos** | **−1.000 (180°)** | **+0.614 (52°)** | 0.201 | 0.389 – 0.954 | K_DIH 500 |

**三个局部坐标（P-P 键、P-C4'、C4'-N）都对得很好** —— 偏移 ≤ 0.007 nm，力 ≤ 3.5 kJ/mol/nm。
骨架记账没问题。**错的全是导出的长程目标：**

1. **`STACK_R0 = 0.505 nm` 是真实 P(i)-P(i+2) 距离的 46%。** 191 个结构上实测 1.112，
   逐结构 sd 只有 0.052，5–95% 完全不覆盖 0.505。这不是折叠差异，这个常数就是错的。
   力 = 500 × 0.607 = **303 kJ/mol/nm**，把每一对 i,i+2 拉近到一半距离 —— 74 对。
2. **`DIH_PPPP = 180°`（cos = −1）而天然是 52°（cos = +0.614）。** 注释写"180° A-form
   dihedral"，但 A-form 是**螺旋**；180° 是伸展链。偏移 1.614，占余弦全量程的 81%。
3. **~~PAIR_NN 对 N–N 和 P–P 都不对~~ —— 这条我说错了，收回。**
   我最初只在 1EHZ 上量到 N–N = 0.875，就断言目标错了。`measure_pair_targets_on_training_set.py`
   在 191 个结构、5307 个 WC 对上重量（判据：C1'-C1' 9.0–11.5 Å + 互补，**这个判据是定义不是测量**）：
   **N–N = 0.954 ± 0.115 nm，逐结构 sd 0.054，目标 1.000 落在分布之内。** 0.875 是 1EHZ 的低端值。
   P–P = 1.653 ± 0.293，确实不是 1.000，但那一项的 sigmoid 在 1.65 nm 处已饱和到 sig = 0.037，
   力只有约 18 kJ/mol/nm，无关紧要。
   **所以 `PAIR_NN` 不是缺陷。bpp 的问题在前因子 `K_BPP/0.3 = 2000`：softplus 在 x = 0 处的
   导数就是 sigmoid(0) = 0.5，也就是珠子正好落在目标距离上时，这一项仍然施加约
   `2000 × 0.5 = 1000 kJ/mol/nm` 的吸引。它在整个合理距离范围内几乎不衰减 —— 这是量级问题，
   不是目标问题。**
4. `_dihedral_f` 本身还带注释承认是**近似梯度**（"The full formula is too complex"），
   并且用 0.25 的任意系数把力分给 b0/b2 —— 这一项既定错目标又是近似力。

**结论：加帽是在治症状。** 真正的问题是 **`STACK_R0` 和 `DIH_PPPP` 两个常数不符合天然 RNA
几何**（外加 bpp 的前因子过大），所以**结构越接近正确，力场推得越狠、方向越错**。
`cg_energy_forces` 用 200 的帽子把它盖住，`cg_forces_explicit_batched` 没盖。

**这一轮我错了两次，都记在上面**：单位换反（§3i），以及把只在 1EHZ 上量到的 N–N = 0.875
当成普遍值、据此断言 `PAIR_NN` 错了。教训是同一条 —— **单结构的数不能当常数用**，
P-only 的四项我在 191 个结构上复核过，配对那两项一开始没有，一复核就翻了。

这也解释了 §3i：CG only 把一条误差 0.48 Å 的 P 轨迹推到 1.892 Å，正是 STACK_R0 把骨架
折半、DIH_PPPP 把每四残基拉平的结果。

**没有动任何常数。** 这是力场的物理，改之前要先定标定方案。


### 3k. 重定标的实测：方向修好了，量级没修（补测）

`scripts/recalibrate_ff_targets.py`。**珠子直接从 rsRNASP 训练集的沉积坐标取**（P / C4' / N9-N1），
不经过我们的重建，所以这一节不依赖我们自己的模板代码。

设计：32 个结构，**前 16 个拟合常数，后 16 个留出测试** —— 所有报告的数字都在留出集上。
每个结构配 6 个高斯扰动的诱饵（σ = 0.3 / 0.6 / 1.0 Å，各 2 个），能量走 `cg_energy_forces`。

常数不是手填的，是从拟合集**重新导出**的：对 `0.5*k*(q-q0)²` 形式的限制项，最优 q0 是观测量的**均值**；
对约束 cos 的项，是观测**余弦**的均值，不是平均角的余弦。

| 设置 | 中位 \|F\| | 在帽上 | native rank | 最好的诱饵比 native 低 |
|---|---|---|---|---|
| A 原样（STACK_R0 0.505，DIH 180°） | 187.5 | 1396/2541 | **6.94 / 7** | **13264.8 kJ/mol** |
| B 重定标 stacking + dihedral | 139.2 | 1204/2541 | **2.00 / 7** | 363.0 |
| C = B 再把 P-P-P 角一起重定标 | 134.2 | 1171/2541 | 2.00 / 7 | 377.9 |

重导出的值：`STACK_R0` 0.505 → **1.152 nm**；`DIH` cos −1.000 → **+0.577**；`ANGLE` cos −0.866 → −0.707。

**第一条结论，也是这一节最重的一条：按出厂常数，这个力场认为天然结构比它自己的随机扰动还差。**
native 在 7 个候选里平均排 6.94，最好的诱饵比它低 13,264.8 kJ/mol。这不是"力大"，是**符号错了** ——
力场的最小值不在天然结构上。上一节那两个常数错得离谱，正是原因。

**第二条：只重定标这两个常数，排序就修好了。** 6.94 → 2.00，margin 13,264.8 → 363.0。
留出集上成立，不是拟合出来的。

**第三条：量级没修好。** 中位力 187.5 → 139.2，在帽上的珠子 1396/2541 → 1204/2541（55% → 47%）。
原因在下面的拆项里一目了然 —— **硬帽子把改善吃掉了**：逐项力降了 30%，中位向量只降了 26%，
剩下的一半仍然超过 200。

留出集上的逐项均值（kJ/mol/nm）：

| 项 | A 原样 | B 重定标 | 变化 |
|---|---|---|---|
| **bpp** | **263.96** | **263.96** | 0 |
| dihedral P-P-P-P | 243.31 | 88.54 | **−154.77** |
| **angle P-P-P** | **131.88** | **131.88** | 0 |
| stacking P-P | 107.34 | 25.46 | **−81.88** |
| bsj closure | 19.55 | 19.55 | 0 |
| wc pair N-N | 15.01 | 15.01 | 0 |
| bb bond P-P | 11.45 | 11.45 | 0 |
| pair guide | 9.72 | 9.72 | 0 |
| 其余 5 项 | ≤ 3.8 | ≤ 3.8 | 0 |
| 逐项均值之和 | 806.01 | 569.36 | −236.65 |

（"逐项均值之和"是各**项**平均力大小的和，不是向量和，只能当粗略聚合看。）

**第四条：现在最大的一项是 bpp，没碰过。** 263.96，比修好的 dihedral 还大。它的前因子
`K_BPP/0.3 = 2000` 在目标距离处就给约 1000，是 §3j 里点过的那个量级问题。

**第五条：angle 不该动。** 变体 C 在留出集上和 B 打平（都是 2.00），margin 反而略差
（377.9 对 363.0）。150° 这个目标虽然不匹配平均余弦，但它在线；改了没好处。
**所以只改两个常数，第三个放着。**

**关于库的状态：一个字都没改。** 重定标是通过在运行时重新绑定模块常数做的
（`C.STACK_R0` / `C._R0_STACK` / `C.DIH_PPPP`）。要注意 `_R0_STACK` 是 1181 行的
import 时快照，而 `DIH_PPPP` 是调用时读取 —— 真要改库，这两个的改法不一样，
只改一个会出现"两条路径用不同常数"的新缺陷。

**这一节的弱点，明说：**
1. 诱饵是高斯扰动珠子，**物理上不成立**（键被拉断、有碰撞）。所以"native 赢"这个测试
   有一部分在考键长键角项，不能当作完整的折叠判别力测试。
2. 拟合/留出只做了一次二分（16/16），没有交叉验证，没有多个随机划分。
3. 用的是 `cg_energy_forces` 的能量，它的 GB/SA 与 Manning 项在两条路径上不一致（§3j），
   这里的能量比较没有把那个 18% 的差异排除掉。


### 3l. 现成方案：文献里的做法，以及我们该走哪条（讨论）

**先说我上一节的错。** §3k 里我把目标设成观测量的**均值**，理由是"均值最小化均方误差"。那个推导对，
但目标错了 —— 均值只在分布对称时才是一个"分子真会待的地方"。伪二面角的分布是重度偏斜的，
`scripts/check_bonded_distributions.py` 一看就清楚：

| 坐标 | 力场目标 | 均值 | **众数** | 落在众数 |
|---|---|---|---|---|
| P-P-P-P cos | −1.000 | +0.608 | **+0.975** | 35.0% |
| P-P-P cos | −0.866 | −0.740 | **−0.875** | 22.9% |
| P(i)-P(i+2) nm | 0.505 | 1.110 | **1.125** | 29.8% |

**众数才是力场该定到的地方**：它是分子实际所处的状态，而均值可能落在两个状态之间。
逐结构的众数更硬（243 条链）：**240 条自己的伪二面角众数在 cos > +0.8，只有 1 条低于 −0.8**；
235 条 P-P-P 众数低于 −0.8；**0 条** i,i+2 距离众数低于 0.7 nm。

带符号看（`scripts/check_signed_pseudotorsion.py`，n = 10631）：**均值 −12.90°，中位 −16.62°**，
56.1% 落在 0° 的 ±30° 内，只有 5.1% 落在 180° 附近，74.6% 为负。
也就是说 P-P-P-P 伪二面角实际在 **−20° 附近（近顺式）**，力场写的是 180°（反式）—— 偏了将近 180°。
用众数重跑（`recalibrate_ff_targets.py`，16 拟合 / 16 留出）：

| 设置 | 中位 \|F\| | 在帽上 | native rank | margin |
|---|---|---|---|---|
| A 原样 | 187.5 | 1396/2541 | **6.94** | −13264.8 |
| B 众数 | 127.6 | 1191/2541 | **1.44** | −210.1 |
| B' 均值 | 139.2 | 1204/2541 | 1.88 | −346.5 |
| C 众数 + angle | 129.0 | 1195/2541 | **1.12** | −35.5 |

**B 与 C 的差别只有 0.009 的目标余弦，margin 却从 −210 变到 −35。这个敏感度不正常**，
n = 16 下 1.44 与 1.12 分不开。只有"A 是灾难性的"这一条在噪声之外。

**文献里现成的方案：有，而且只有一种，方法是标准的。**

1. **Boltzmann inversion（玻尔兹曼反演）** —— 键合项的标准做法，`U(q) = -kBT ln P(q)`，
   P 从结构数据库统计得到。**迭代版 IBI** 在 MD 与反演之间来回迭代，修正项与项之间的关联。
   文献：Li W, Chen S-J，*Building RNA Coarse-Grained Force Fields: Design Principles and Training
   Strategies*，Biophys J 2026，doi:10.1016/j.bpj.2026.03.041（PMC13110076，PMID 41877485）。
   该文的训练方法小节标题就是 "Boltzmann Inversion of reference probability distributions" 与
   "Iterative Boltzmann Inversion (IBI)"。
2. **该文对"做天然结构预测的模型"的判据，和我们正在做的测试是同一件事**：SimRNA / IsRNA2 / RNAJP
   "often adopt training strategies to reproduce detailed statistical distributions of structural
   parameters. Validation of such models is achieved primarily by their ability to search for and
   identify native conformation." —— 我们用 native vs 诱饵的排序来验证，正是这个标准。
3. **有一份公开数据集的先例**：*Dataset of normalized probability distributions of virtual bond
   lengths, bond angles, and dihedral angles for the coarse-grained single-stranded DNA structures*
   （PMC9142626；Mendeley nbd83424kc/2）。是 ssDNA 不是 RNA，但配方一样 —— 说明这份表是要公开的
   标准产物，RNA 版我们可以自己从训练集统计。
4. **但不能整体照搬任何模型** —— 珠子定义不匹配。据同一篇综述：NAST 是**每残基一个珠子，放在 C3'**；
   oxRNA 是刚体多作用位点；SimRNA / RNAJP 是五珠；IsRNA2 是**五珠/核苷酸**（PMC9731381 摘要原话
   "five-bead per nucleotide coarse-grained representation"）；Martini 3 RNA 是 4–7 珠。
   和我们同为三珠 P/C4'/N 的只有 cgRNASP，而它是**打分函数，没有键合项**。
   **结论：方法可以照搬，参数不能。**

**三条路：**

**路 1 —— 最小改动：把目标改成众数，保留调和形式。**
改 `STACK_R0` 0.505 → 1.125 nm、`DIH_PPPP` 180° → acos(+0.975) = 12.8°。
`ANGLE_PPP` 本来就对（150° 的 cos −0.866 对众数 −0.875），不动。
改动是三行，可回退，留出集上已测（6.94 → 1.44）。
**缺点：调和项表达不了偏斜。** 众数那一格只装得下 35% 的观测，剩下的尾巴全被当成错误惩罚。

**路 2 —— 玻尔兹曼反演（真正的解法）。**
把键合项从"目标值 + 弹簧"换成**制表的** `U(q) = -kBT ln P(q)`，再用 IBI 迭代。
这是文献的标准做法，能表达偏斜和多峰，不需要挑统计量。
**关键：这套机器我们已经有了。** §3g/§3h 为 cgRNASP 做的**可微制表势求值器**
（bin 中心线性插值 + 解析梯度，与中心差分吻合到 9e-10）就是干这个的。
所以这不是写新代码，是把已经验证过的代码换个用途。
训练集也已经在解析了。代价：建 4 张表、接进去、重跑留出集测试。

**路 3 —— 整体换用现成模型（NAST / oxRNA / SimRNA / Martini 3）。**
被珠子定义挡住。换了就要重写 CG 阶段、重建、整条流水线。不成比例。

**我的意见：路 1 先做（解锁），路 2 是真正的答案，做路 2 之前先读那篇综述的训练小节。**

**还有一件事要一起想。** 这一整条线（§3 全部）起于"怎么把统计势注入力"。
现在反过来了：**统计势本来就是该用的东西，但该用在键合项上，而我们自己的键合项是错的。**
如果我们已经在建 `-kBT ln P(q)` 表，那"注入 cgRNASP"就不再是一个独立任务 —— 它是同一套机器的
另一个应用。§3i 那个 p = 0.077 的松弛结果，是因为我们在一个坏力场上测外来的势。

**仍未解决的，独立于上面：**
- 两条库路径能量差 18%（53,711.73 vs 45,441.82 kJ/mol），生产路径没有力帽。
- 常数改对之后要重看力帽：原始力降下来，帽子或许就不用当了力律。
- **bpp 是现在最大的一项**（263.96 kJ/mol/nm，前因子 2000），没碰过。


### 3m. 路 1 落地（已改库）

**改动，三处，都在 `src/torusfold/scheme2/torch_cgsim.py`：**

1. `STACK_R0` 0.505 → **1.125**（P(i)-P(i+2) 的众数）。
2. `DIH_PPPP` `math.pi * 180.0 / 180.0` → **`math.acos(0.975)`**（伪二面角余弦的众数）。
3. **删掉 `_R0_STACK = STACK_R0` 这个 import 时快照**，两处使用改为直接读 `STACK_R0`。
   注释留在原位，写明为什么这里故意没有快照。

`ANGLE_PPP` 未动。

**验证（`scripts/recalibrate_ff_targets.py 64`，32 拟合 / 32 留出）：**

| | 中位 \|F\| | 在帽上 | native rank | margin |
|---|---|---|---|---|
| 改动前（`HEAD`） | 192.5 | 3934/6981 | **6.75** | −19592.6 |
| **改动后（出厂值本身）** | **151.5** | 3313/6981 | **1.19** | **−90.2** |
| 从拟合集导出的众数（B） | 151.5 | 3313/6981 | 1.12 | −47.8 |

出厂值 `STACK_R0 1.125`、`DIH cos +0.975` 与从数据导出的众数完全一致。

**那个快照 bug 是真的，而且被证明了。** `scripts/prove_snapshot_bug_was_real.py` 把改动前的
文件从 git 取出来单独加载，跑同一条行为检查：

```
old revision, changing STACK_R0:
  cg_energy_forces            responds: True
  cg_forces_explicit_batched  responds: False
```

也就是说，如果当初只改 `STACK_R0` 而不动 `_R0_STACK`，**一条路径会修好，另一条会静默地继续用
旧值** —— 不报错、不警告，只是两条路径不一致。`tests/test_ff_bonded_targets.py` 里有一条
测试专门盯这个（改常数，要求两条路径的**能量都跟着变**）。

**测试：`python -m pytest tests/` → 7 passed, 2 failed。**
两个失败是 `tests/test_force_gradcheck.py`，而且**改动前就是红的**，
用 `scripts/attribute_gradcheck_failure.py` 在同一个系统上对新旧两版各跑一次：

| 力函数 | 改动前 max_rel | 改动后 max_rel |
|---|---|---|
| `cg_forces_explicit_batched` | **3.4610** | 4.2316 |
| `cg_energy_forces` | **50.7830** | **11.4201** |

容差是 0.2。**两个在 `HEAD` 上就已经远超容差，不是这次改动造成的**；
这次改动让 `cg_energy_forces` 的偏差降低 4.4 倍，另一个基本没动（且略差）。
这是一个独立存在的既有缺陷 —— 力与自身能量的梯度不自洽，与 `_dihedral_f`
注释里自承的"近似梯度"（613 行）以及 628–629 行那两个 0.25 的任意系数吻合。

**新发现的、尚未处理的：`openmm_gpu_refiner.py` 有自己一套常数，与 torch 不一致。**

| | `torch_cgsim.py` | `openmm_gpu_refiner.py` |
|---|---|---|
| 二面角目标（同一组原子 P(i)-P(i+1)-P(i+2)-P(i+3)） | 180°（已改 → 12.8°） | **33°**（171 行） |
| 堆叠项**原子对** | P(i)-P(i+2) | **N(i)-N(i+1)**（418 行） |
| 堆叠目标 | 0.505（已改 → 1.125 nm） | **5.05 Å** |

**同一组原子、两个相差 147° 的目标**；而堆叠项是**不同的原子对**却写着同一个数字
（5.05 Å = 0.505 nm）—— 那就是这个 bug 进来的方式：常数被抄了过去，抄的是另一对原子。
实测 `N(i)-N(i+1)` 是 **0.566 ± 0.201 nm**，所以 5.05 Å 那一侧偏 0.061 nm，属于轻度。
**这次没动 OpenMM 侧** —— 我的测量无法在 12.8° 与 22.5° 之间干净地取舍（见下），
把它从 33° 改成 12.8° 未必是改善。这一项留到路 2，那时整条分布会取代单个目标值，歧义自然消失。

**一个必须记住的警告：那个排名指标在细粒度上是噪声。**
出厂值与拟合值只差约 1e-6 的两个常数，rank 却差了 0.07（1.19 对 1.12）—— 32 个结构里有 2 个翻转。
所以 §3k/§3l 里 B / B2 / C 的排序（1.12 / 1.25 / 1.19）**都在噪声内，不构成结论**。
稳的只有一条：**旧值 rank 6.75 / margin −19592.6 是灾难性的，改成众数后落到 1.1–1.5**。

**仍然没有解决：**
- 量级没修好：中位力 192.5 → 151.5，在帽上的珠子 3934/6981 → 3313/6981（56% → 47%）。**帽子还在当力律。**
- **bpp 仍是最大项**（277.14 kJ/mol/nm，前因子 `K_BPP/0.3 = 2000`）。
- 两条库路径能量差 18%，生产路径没有力帽。
- gradcheck 的既有失败（上表）。
- `openmm_gpu_refiner.py` 与 torch 的常数分歧（上表）。


### 3n. 路 2 的配方（从综述抄下来的，含两个正式名字）

Li & Chen 那篇综述（Biophys J 2026, PMC13110076）的 Table 1 把 CG 参数化方法列成一张表。
和我们相关的两条，都属于 bottom-up：

**DBI —— Direct Boltzmann Inversion。**
`U(q) = -kBT ln P_ref(q)`，P_ref 是参考分布。
优点："Straightforward in concept and implementation."
缺点："**Does not account for correlations between different degrees of freedom.**"

**IBI —— Iterative Boltzmann Inversion。**
在 MD 与反演之间迭代，`U_{i+1}(q) = U_i(q) + kBT ln(P_i(q) / P_ref(q))`。
优点："Can account for correlations between different degrees of freedom."
缺点："**Convergence not always guaranteed.**"

**IsRNA 用的是改良 IBI**，这一点值得抄：它不从全部参数一起优化，而是**分批加**。先只有键长和
体积排斥（LJ），假设键长互相独立；其余参数在后续迭代里逐步加入。原文：

> the individual potential terms extracted by the IsRNA procedure depend on the order they are
> added to the training process. ... However, **the sums of the energy terms in the final force
> fields are not sensitive to the iteration orders.**

也就是说，**一项一项加是有授权的，先加键长不会把后面的搞坏。**

**整条 §3 线的理论落点，综述里有原话：**

> In DBI and IBI methods, it is common practice to use experimental structural distributions as
> references. Potentials obtained in this manner are sometimes referred to as **'statistical
> potentials'**. ... One way of thinking about the statistical potentials is to treat them as
> scoring functions, reflecting the preferred values adopted by different degrees of freedom in
> the native configurations of RNA molecules.

拿实验天然分布建的 CG 势，本来就叫统计势。§3 起于"怎么把统计势注入力"，绕了一圈，
**这就是标准做法本身。**

**我们的检验也有名字。** Top-down 那半边有一条 **Energy Funnel Optimization**：
"Requires energy landscape to adopt funnel shape" / "The energy landscape should guide folding
toward native state" / "**Requires the generation of a well-designed set of decoy structures**"。
就是 native vs 诱饵排序。而 §3i 那种松弛对应 **RMSD Minimization**，综述给的缺点原话是
"lacks analytical gradient, and requires the use of less efficient numerical derivatives" ——
和我们测到的 p = 0.077 是同一件事。

**所以：我们的计划是标准计划，我们的检验是标准检验，而 §3k 测出的那个失败**
（力场把天然排在 6.75/7）**正是 Energy Funnel Optimization 存在的理由。**

**路 2 的具体步骤：**

1. 对每个键合坐标（P-P 键、P-C4'、C4'-N、P-P-P 角、P-P-P-P 伪二面角、P(i)-P(i+2) 距离），
   从训练集统计 `P_ref(q)`。
2. `U(q) = -kBT ln P_ref(q)`，制表，用与 §3h 同一套 bin 网格。
3. 用**已经验证过的**可微制表求值器算能量与力（bin 中心线性插值 + 解析梯度，
   与中心差分吻合到 9e-10）。不需要写新代码。
4. **尾巴要加墙。** `-ln P` 在采样范围之外没有定义，插值会把它拉平 —— 也就是"自由"，
   坐标可以无代价地跑到分布之外。综述没谈这一条，这是我自己要处理的地方：
   需要在 `P_ref` 的支撑边界外接一个上升的墙，否则同样的塌陷会以另一种形式回来。
5. 重跑留出集上的 Energy Funnel 检验（`scripts/recalibrate_ff_targets.py` 的框架，
   换成制表势）。
6. DBI 的已知弱点是不管自由度之间的关联，而键合项确实互相关联 ——
   **所以大概率要走 IBI**。IBI 需要 MD 在环里，是个大得多的循环。
   综述也说了 "Convergence not always guaranteed"，所以先 DBI 看差多少，再决定。

**先做 1–3，用 DBI，跑第 5 步。** 如果 DBI 的分布复现不了参考分布，再谈 IBI。


### 3o. 路 2 第一步：玻尔兹曼反演的键合势做出来了，而且通过了漏斗检验

**代码：`scripts/boltzmann_bonded.py`（模块）、`scripts/test_boltzmann_bonded.py`（自检）、
`scripts/funnel_test_boltzmann.py`（检验）。**

六条键合坐标各建一张 `U(q) = -kBT ln P_ref(q)` 表：`bb_bond`（P-P 键）、`intra_pc`、
`intra_cn`、`angle`（P-P-P 余弦）、`dihedral`（P-P-P-P 余弦）、`stack`（P(i)-P(i+2)）。
120 格，伪计数 0.5（没有它 `-ln 0` 是无穷，插值器无从插起），能量整体平移使最小值为零。

**力用自动微分取，不手推梯度。** 理由写在模块开头：`torch_cgsim._dihedral_f` 自带注释承认
它的梯度是近似（"The full formula is too complex"），而 `tests/test_force_gradcheck.py`
在 `HEAD` 上就是红的。在这里再推一个近似梯度就是重犯同一个错。坐标函数写成普通可微 torch，
梯度按构造就是精确的。

**表本身的验证：`U` 的最小值落在每一维数据的众数上。**

| 坐标 | n | 数据众数 | argmin U | 空 bin | 旧调和目标 |
|---|---|---|---|---|---|
| bb_bond | 1356 | 0.587 | 0.580 | 76/120 | 0.590 |
| intra_pc | 1388 | 0.392 | 0.392 | 63/120 | 0.390 |
| intra_cn | 1388 | 0.336 | 0.341 | 28/120 | 0.335 |
| angle | 1324 | −0.868 | −0.859 | 22/120 | −0.866 |
| **dihedral** | 1292 | **0.983** | **0.997** | 10/120 | 0.975 |
| **stack** | 1324 | **1.124** | **1.116** | 50/120 | 1.125 |

**墙是必须的，而且原因很具体。** `-kBT ln P` 只在采样范围内有定义；最外几格是空的，
伪计数让它们数值齐平，所以**表在边缘的斜率就是 0**，直接插值会把坐标"免费"放出去。
加了一堵 C1 连续的二次墙，锚在边缘值和边缘斜率上：

| 坐标 | 边缘 U | 越界 20% 处的 U | 边缘斜率 | 越界 20% 处斜率 | 力指回内部 |
|---|---|---|---|---|---|
| bb_bond | 15.40 | 30.22 | 0.00 | 76.98 | 是 |
| intra_pc | 14.97 | 15.08 | 0.00 | 6.55 | 是 |
| intra_cn | 12.00 | 12.02 | 0.00 | 3.04 | 是 |
| angle | 13.02 | 26.49 | 0.00 | 73.40 | 是 |
| dihedral | 14.67 | 31.98 | 0.00 | 83.20 | 是 |
| stack | 14.49 | 37.67 | 0.00 | 96.30 | 是 |

自动微分的力对中心差分：**最差相对误差 6.0e-09**（6 个探针）。

**漏斗检验（Energy Funnel Optimization）：32 结构拟合，32 结构留出，每个 6 个诱饵。**

| 能量 | native rank | margin | native \|E\| 量级 |
|---|---|---|---|
| harmonic（已重定标的调和项） | 1.22 | −88.9 | 7550.2 |
| **boltzmann（六项全用 -kBT ln P）** | **1.00** | **0.0** | 782.2 |
| full（整场，含非键合） | 1.25 | −99.9 | 8344.8 |
| swapped（整场但键合项换成玻尔兹曼） | 1.06 | −11.1 | 13538.2 |

**留出的 32 个结构里，每一个的天然构象都是 7 个候选里最低的那个，没有任何诱饵赢过一次**
（rank 1.00，margin 精确为 0）。而"调和项最优版本"是 1.22 / −88.9。换成整场之后，
1.25 → 1.06，−99.9 → −11.1。

**三条必须说清楚的保留意见：**

1. **诱饵是不物理的。** 高斯扰动珠子会把键拉断、造出碰撞。所以这个检验在某种意义上偏软：
   一个能正确反映"哪些几何是常见的"的势，天然就会给扰动过的结构更高的能量。
   **需要更硬的诱饵**（别的折叠、别的序列的天然结构），否则不能声称这是折叠判别力。
2. **只做了一次 32/32 二分**，没有交叉验证。而且上一节已经证明这个排名指标在细粒度上敏感
   （1e-6 的常数差异能翻 2/32），所以**不要读 1.00 与 1.06 之间的差**。
   能读的是：**1.00–1.06 对 1.22–1.25**。
3. **量级不匹配，这不是等价替换。** `-kBT ln P` 的深度只有几 kBT（六张表的全域跨度是
   12–15 到约 40 kJ/mol 量级），而 k ≈ 500 的调和项在零点几纳米的偏差上就给几百 kJ/mol。
   表里的 native \|E\| 一列就是为这个放的：boltzmann 782.2 对 harmonic 7550.2，差一个数量级。
   换进整场之后非键合项仍然主导。**"要不要给玻尔兹曼项定标"这个问题没有被这次结果回答**，
   反而被这次结果变得更要紧。

**没有动库。** 玻尔兹曼势目前只在脚本里，`torch_cgsim.py` 用的还是 §3m 那套调和项。


### 3p. 定标：漏斗管不了标度，而调和常数之间本来就不同源（补测）

**先说我这次的错。** 第一版 `scales[name] = 1.0 / ratio` 写反了。要让缩放后的曲率等于调和 k，
应当乘 `k / U''`，我乘了它的倒数 —— 于是"曲率配平"那一行给出 179,890 kJ/mol 的荒谬值，
而它本该是 125.3。已修。

**测出来的曲率，以及它为什么只是指示性的。**

| 坐标 | 调和 k | 拟合 U'' | kBT/σ² | k / U'' |
|---|---|---|---|---|
| bb_bond | 500.0 | 2464.2 | 157.9 | 0.203 |
| intra_pc | 400.0 | 418344.3 | 18379.4 | **0.001** |
| intra_cn | 400.0 | 409725.0 | 29825.7 | **0.001** |
| angle | 600.0 | 326.8 | 27.5 | **1.836** |
| dihedral | 500.0 | 7757.1 | 6.9 | 0.064 |
| stack | 500.0 | 953.0 | 81.6 | 0.525 |

单位不用换算：U 是 kJ/mol，距离坐标用 nm，角度坐标约束 cos，所以 U'' 与 k 直接可比。

**这张表说明两件事，第二件是关于我自己的方法的。**

1. **调和常数彼此不同源。** 同一个 k 的量级（400–600）在六条坐标上对应的真实曲率相差
   **三个数量级**（angle 1.84 倍，intra_pc 0.001 倍）。这些 k 是调出来的数字，不是从同一套
   物理或同一份数据导出的。这就是"定标"问题的具体形态。
2. **但拟合的 U'' 本身不可靠。** 对照 `kBT/σ²`（同样宽度的抛物线阱应有的曲率）：
   bb_bond 2464 对 158，intra_pc 418344 对 18379，**dihedral 7757 对 6.9** —— 差了一千倍。
   窄坐标上抛物线是在拟合一个分格很粗的阶梯，不是在测曲率。
   所以第 1 条应读作"存在很大的离散"，不是"精确测到了这个比值"。**"曲率配平"因此不是一个
   有根据的判据** —— 拿它当标定标准会把 intra_pc/intra_cn 缩到 0.001 倍，等于删掉这两项。

**漏斗对标度不敏感。**

| 倍数 | native rank | 平均 gap (kJ/mol) |
|---|---|---|
| 0.03 | **1.000** | 0.0 |
| 0.10 | **1.000** | 0.0 |
| 0.30 | **1.000** | 0.0 |
| 1.00（曲率配平） | 1.031 | −0.1 |
| 3.00 | 1.062 | −0.2 |
| 10.00 | 1.062 | −0.8 |
| 30.00 | 1.062 | −2.5 |
| **调和项** | **1.250** | **−41.2** |

倍数是相对于曲率配平后的表。**在 1000 倍刚度范围内，rank 全部贴在 1.000 的天花板上**
（≤ 0.3 时 gap 精确为 0；到 30 倍也只有 −2.5 kJ/mol），而调和项是 1.250 / −41.2。

**所以：**
- **形状是对的，标度是漏斗看不见的。** §3o 那个 1.00 不是"更软的势更好"—— 同一个形状从
  0.03 倍到 30 倍都能拿 1.000，而任何倍数的调和项都拿不到。差别在形状。
- **标度不能用漏斗定，必须换一个机制。**
- **不能把"曲率配平"当答案**（第 2 条已经排除）。

**结论：保持 DBI 的字面标度（倍数为 1，表就是平均力势），把标度交给 IBI。**
理由：（a）没有任何测量支持乘一个系数；（b）没有任何可用的检验能挑出系数；
（c）**IBI 恰恰就是干这件事的** —— 它迭代到模拟出来的分布复现参考分布为止，
而"复现参考分布"本身就同时定住了形状和标度。

**这一步把 IBI 从"可能要做"变成了"必须要做"**：它是目前唯一一个能用证据定标度的机制。


### 3q. 两个力 bug、一处死代码，以及一个测不动的帽子

**先纠正我自己前面的说法。** §3j 里我称 `cg_forces_explicit_batched` 为"生产路径"，
并据此说"生产路径没有力帽，力到 26,086"。**它是死代码。** 全仓库对它只有：它自己的定义、
我写的探针、根目录一个独立的 `verify_force_fd.py`、以及未跟踪的 `old_torch_cgsim_ref.py`。
`src/` 里没有任何调用者。活路径是 `cg_energy_forces`。

而两条路径的"18% 差异"也不是数值问题，是**两个不同的力场**（`scripts/attribute_path_disagreement.py`
逐项实测）：

| 项 | unified 有？ | batched 有？ |
|---|---|---|
| bpp | True | **False** |
| pair guide | True | **False** |
| BSJ contact | True | **False** |
| Mg / `_K_MG` | **False** | True |
| GB 系数 | `2.0 * 0.73 * Σ` | `0.365 * Σ`（**整 4 倍**） |

`_K_BPP`、`_K_PAIR_GUIDE`、`_K_BSJ_CONTACT` 定义在 1187–1189 行，**除定义外无任何使用** ——
三项被丢下时留下的孤儿常数。

**然后是真正要紧的：活路径有两个力是错的。**

`scripts/gradcheck_per_term.py` 逐项做有限差分（用真结构，不用 `torch.rand` 的噪音几何）。
最初的十三项：

| 项 | max \|F\| | 最差相对误差 |
|---|---|---|
| **angle P-P-P** | 841.73 | **1.927** |
| **dihedral P-P-P-P** | 1729.60 | **1.836** |
| **bsj contact** | 0.29 | **1.000** |
| 其余十项 | — | ≤ 5.6e-07 |

`scripts/diagnose_angle_gradient.py` 独立推了一遍角度梯度并按同样方式验：

| 梯度 | 最差相对误差 |
|---|---|
| 库里 `_angle_f`（原） | **1.927e+00** |
| 教科书形式 | **6.196e-08** |

两者力的**相关系数 −0.9296**。**角度项 —— 最大的力贡献者之一 —— 在把自己的能量梯度推反。**
原因是那两行 `dcos_dx` 的领头项符号反了，cos 项还多带一个 `n2`。

二面角那一项库里自带注释承认是近似（"The full formula is too complex"），用 0.25 的任意系数分力。
bsj contact 的力符号反了，且多除一个 `rc`（量级只有 0.3 kJ/mol/nm，所以一直没被注意到）。

**三处都修了。修完十三项全部自洽，最差 5.58e-07。**

**但修完之后出现了一件更大的事。** 二面角的最大力从 1729.60 跳到 **31132.49** ——
能量表达式没变，FD 通过，所以那个梯度是真的，**是原来那个错误的梯度一直在意外地压住这个量**。
`scripts/diagnose_dihedral_force_scale.py` 在 8 个结构、306 个窗口上量：

| | |
|---|---|
| 力超过 200 kJ/mol/nm 帽子的窗口 | **281 / 306（91.8%）** |
| 中位 \|F\| | **2211.3** kJ/mol/nm（帽子的 11 倍） |
| 最大 \|F\| | **32671.8** |

原因是函数形式：`E = 0.5*k*(cos φ - c)²` 的梯度量级正比于 `k|cos φ - c| / (b sin φ)`，
在 φ→0 与 φ→180 处发散。§3m 把 c 从 −1.0 挪到 +0.975，等于把目标从一个奇异点
（180°）挪到另一个奇异点（0°）—— 而那正是数据所在的地方。

**所以力帽现在几乎处处生效**，而帽子是对输出向量的非线性缩放 —— 那一步之后 `F = -dE/dx`
**按构造就不成立**，与哪一项对不对无关。这解释了为什么 `tests/test_force_gradcheck.py`
在十三项全干净之后仍然不过。

**这一条我测不动，如实说。** `scripts/diagnose_cap_vs_gradcheck.py` 试图用
"顶在帽子上的珠子比例"去解释 max_rel，失败了：`max_rel` 的分母是某分量上的力，可以接近零，
这个统计量本身会炸（1D4R 有 17% 被帽，max_rel 却是 3720）。要真正判定，需要把那个帽子
变成显式参数，才能关掉它做对照。**那是另一处改动，我没有做。**

**最后一条，关于检验的盲区：** 漏斗检验读的是**能量**，而这四个修正改的全是**力**。
所以 `funnel_test_boltzmann.py` 的输出在修正前后**一字未变**（harmonic 1.22 / −88.9，
boltzmann 1.00 / 0.0，full 1.25 / −99.9，swapped 1.06 / −11.1）。**漏斗对力的正确性完全瞎。**
判断这些修正有没有用，需要的是动力学的检验，不是能量的。

**还有一处我自己的模块的弱点**，一并记下：`cg_force_terms.py` 对库里**有 helper** 的项
（`_bond_f`、`_angle_f`、`_dihedral_f`、`_clash_f`、`_sigmoid_f`）是调用，
对**没有 helper** 的项（BSJ closure、stacking、WC pair、pair guide、bpp、BSJ contact）
是**复制**。它会漂移，而且已经漂了：库里的 BSJ contact 修好之后，这个副本还是旧的，
所以 gradcheck 测的是副本不是库。需要权威判断时应直接读 `torch_cgsim`。


### 3r. 我给的机制是错的；而且修不了这个病（补测）

**§3q 里我写的"梯度正比于 `k|cos φ - c|/(b sin φ)`，在 φ→0 与 φ→180 处发散"是错的。**

重推链式法则：`dE/dx = k(cos φ - c)·(-sin φ)·dφ/dx`，而 `dφ/dx` 的量级是 `1/(|b| sin θ)`，
**θ 是键角不是二面角**。所以 `sin φ` 在**分子**上、会消掉，发散点在**键共线**，不在二面角平面。

实测（`scripts/diagnose_dihedral_large_force.py`，411 个窗口）：

| 量 | 与 \|F\| 的相关 |
|---|---|
| 1/sin(二面角) | **0.082** |
| 1/sin(θ1) | 0.198 |
| \|cos φ\| | −0.182 |

**没有任何几何量单独解释得了它**。而 §3q 说的"把目标从 180° 挪到 12.8° 所以进了奇异区"，
按重推的结果不成立 —— 两个目标都不在奇异点上。

**结论：你批准的那个方案（把约束写在该角 φ 上而不是余弦上）修不了这个病。**
两种形式共用同一个 `dφ/dx`，而发散在那个因子里。**它没做。**

**顺带查出一个确定的缺陷，已修。** `boltzmann_bonded._chain_residues` 只保留 A/C/G/U，
中间的修饰核苷酸被丢掉，于是列表里前后两项在链上可能隔了好几个残基，
却被当成一根骨架键、一个角、三个二面角用。实测 **7354 对相邻项里 93 对（1.26%）是这种间隙**，
步长 2 到 10。**而那正是玻尔兹曼表的分布来源。**

修法是在编号不连续处把链断开。修完验证（`scripts/verify_gap_fix_and_dihedral.py`，60 条链）：
P-P 步长 3061 个，**超过 0.90 nm 的 0 个**，均值 **0.591 ± 0.047 nm** —— 干净的天然带。

**然后是我统计量用错的更正。** 我引用过"二面角中位力 2323.7，是帽子的 11 倍"。
那个数是**逐窗口取四原子最大值、再取中位**，不是逐珠子的中位力。逐珠子的中位是 **3.60**。
两者不是一回事，我混用了。

**正确的分布（`scripts/measure_bonded_force_distribution.py`，24 个清洗后结构，2985 个珠子）：**

| | |
|---|---|
| 中位 | **3.60** kJ/mol/nm |
| p95 | 3506.53 |
| p99 | 10898.61 |
| max | 32753.82 |
| 超过 200 帽子的珠子 | **784 / 2985（26.26%）** |

| 项 | 中位 | p95 | p99 | max | 超帽 |
|---|---|---|---|---|---|
| bb bond P-P | 0.00 | 49.01 | 80.78 | 158.42 | 0 |
| intra P-C4' | 0.49 | 7.41 | 19.62 | 34.03 | 0 |
| intra C4'-N | 0.69 | 7.16 | 10.25 | 13.05 | 0 |
| **angle P-P-P** | 0.00 | **755.09** | 1314.58 | 1936.80 | **375** |
| **dihedral P-P-P-P** | 0.00 | **3217.85** | 10576.79 | **32671.85** | **746** |
| stacking P-P | 0.00 | 90.88 | 226.27 | 429.10 | 46 |

**键合场在中位数上是平静的，在尾巴上是暴烈的，而尾巴整个属于 angle 和 dihedral。**
三个局部项最大才 158 / 34 / 13，全部在帽下。

**那么真正的杠杆是什么。** `scripts/funnel_vs_stiffness.py` 把十一个 k 同乘
0.003 到 3.0（3000 倍），留出集 rank 是 **1.219，小数点后三位完全不变**。

这不是发现，是数学：**排序只取决于各项之间的差，整体缩放对它是恒等的。**
所以"整体调软会不会伤害漏斗"这个问题有一个精确的答案 —— **不会，一个比特都不会变。**
而力帽不是标度不变的：力降下来，帽子就不再当力律。

**这才是杠杆：标度，不是函数形式。**
§3p 在玻尔兹曼表上得到同一个不变性（1000 倍范围内 rank 贴 1.000），是同一个事实的另一个实例。

**所以我把 §3q 末尾那句"下一步是改函数形式"撤回。** 该做的是把 angle 和 dihedral 的刚度
降下来（或者换成深度只有几 kBT 的玻尔兹曼表），这件事对漏斗零代价，对力帽是解药。


### 3s. 重定标：表重拟合了，方差验收失败，而失败的是我的验收设计

**第一步：表用清洗后的数据重拟合**（`scripts/refit_tables_clean.py`，96 条清洗链）。

| 坐标 | n | σ | **kBT/σ²** | 现在的 k | 现在/该值 |
|---|---|---|---|---|---|
| bb_bond | 4880 | 0.0470 | 1129.2 | 500.0 | 0.443 |
| intra_pc | 4976 | 0.0110 | 20752.7 | 400.0 | **0.019** |
| intra_cn | 4976 | 0.0083 | 36399.2 | 400.0 | **0.011** |
| angle | 4784 | 0.2978 | 28.1 | 600.0 | **21.3** |
| dihedral | 4688 | 0.5880 | 7.2 | 500.0 | **69.3** |
| stack | 4784 | 0.1197 | 174.0 | 500.0 | 2.87 |

**跨度 6307 倍**，方向两个都有：三个局部项要变硬 2–91 倍，angle/dihedral 要大改软 21–69 倍。
表的众数仍落在数据众数上（`argmin U` 0.564/0.392/0.337/−0.859/0.997/1.126 对数据众数
0.567/0.392/0.337/−0.868/0.983/1.123），深度 15.3–18.2 kJ/mol ≈ 6–7 kBT。
表存到 `results/boltzmann_tables_clean.npz`。

**一个顺带成立的关系：匹配数据的谐振子，平衡时的力尺度是 `kBT/σ`，与 k 无关。**
算出来：bb_bond 53、intra_pc 227、intra_cn 300、angle 8.4、dihedral 4.2、stack 20.8 kJ/mol/nm。
全部远在 200 的帽子下。

**第二步：混合势建好了**（`boltzmann_bonded.mixed_energy`）：
三个局部项用调和、k = kBT/σ²；angle / dihedral / stack 用表。

**第三步：方差验收。失败。而测试是我设计错的。**

`scripts/sample_bonded_chain.py`，过阻尼 Langevin，P-only（bb_bond/angle/dihedral/stack
四项全是 P 原子的函数，所以 k_max 从 36399 降到 1129，dt 放大 32 倍），12 万步 =
**153 个最软坐标的弛豫时间**：

| 坐标 | 参考均值 | 参考 σ | 采样均值 | 采样 σ | 比值 |
|---|---|---|---|---|---|
| bb_bond | 0.5640 | 0.0470 | 0.6061 | 0.0946 | **2.013** |
| angle | −0.8593 | 0.2978 | **−0.3901** | 0.4765 | **1.600** |
| dihedral | 0.9967 | 0.5880 | **−0.0981** | 0.7739 | **1.316** |
| stack | 1.1255 | 0.1197 | 0.9879 | 0.2103 | **1.757** |

（intra_pc / intra_cn 是解析的，谐振子方差就是 kBT/k，没有可测的东西。）

**这个测试不可能通过，原因不在势，在设计。** 只有键合项的链没有任何理由保持折叠，
而**参考边缘分布是以折叠为条件的**。dihedral 的均值从 +0.997 走到 −0.098，
就是这条链在解开 —— 这是应该发生的事，不是缺陷。一条自由的 RNA 链本来就该是无序的。

**但宽度比值 1.3–2.0 是有信息的**，而且是可解释的方向：耦合让边缘分布变**宽**。
这正是综述说的 DBI "does not account for correlations"，也是 IBI 存在的原因。

**而顺着这条线，我撞到一个更要紧的东西，它推翻了"用 σ 定标"这个方案本身。**

**匹配方差和保持几何是两件冲突的事。** 对 dihedral：σ_cos = 0.588，所以
k = kBT/σ² = 7.2，它在整个余弦范围上的阱深只有 `0.5 × 7.2 × 1.588² = 9 kJ/mol = 3.6 kBT` ——
**3.6 kBT 兜不住一个构象**，热涨落就能把它翻出去。而出厂的 k = 500 给出的阱深是
`0.5 × 500 × 1.588² = 631 kJ/mol = 253 kBT`，兜得住，但过冲。

**根源是 σ 本身。** 数据库里 pool 起来的 σ = 0.588 混了**不同构象态**，
它不是任何单个状态的宽度。拿它当目标，等于要求势把"所有结构的所有状态"都当成一个高斯。

**所以 IBI 不是可选项，是唯一能闭合这件事的机制** —— 这和 §3p 的结论相同，
但理由好得多：不是"标度没人定"，而是**池化的数据库分布本身就不是一个可用的目标**，
必须在真实语境里迭代。`scripts/sample_bonded_chain.py` 的框架就是 IBI 的外循环缺的一半。


### 3t. 消融：angle 和 dihedral 不做有用功，却扛着全部的力

**做法：在六项能量和里按系数缩放被消融的那一项**（`scripts/ablate_backbone_terms.py`），
比改常数干净，也不碰 `_K_*` 快照那类坑。同时量漏斗排名和逐珠子力。

| 设置 | rank | gap | p95 \|F\| | max \|F\| | 超帽 |
|---|---|---|---|---|---|
| full | 1.156 | −102.5 | 3549.1 | 172158.2 | 27.1% |
| dihedral ×0.3 | 1.094 | −34.1 | 1619.6 | 51646.6 | 23.3% |
| dihedral ×0.1 | 1.125 | −16.6 | 1144.8 | 17215.1 | 18.9% |
| dihedral ×0.03 | 1.156 | −11.2 | 1027.8 | 5165.6 | 16.1% |
| dihedral off | 1.156 | −10.4 | 971.1 | 2666.9 | 14.6% |
| angle ×0.3 | 1.156 | −89.3 | 3117.0 | 172161.1 | 26.5% |
| angle off | 1.219 | −84.4 | 3004.7 | 172162.3 | 26.3% |
| **两个都 off** | **1.000** | **0.0** | **133.2** | **297.2** | **1.9%** |
| 三个都 off | **1.000** | **0.0** | 46.7 | 137.0 | **0.0%** |

**单独关掉任何一个，排名几乎不动（1.156 → 1.156 / 1.219）；两个一起关，排名变成满分，
而力尾从 172,158 塌到 297，超帽从 27.1% 掉到 1.9%。** 两个一起才对，说明这对项是冗余的，
而且它们的**冲突**才是漏斗失分的原因 —— 两条限制从不同方向拉同一段局部几何。

**但"两个都关"会留下一条没有弯曲刚度的链**（局部三项只管键长和珠内距离），
那会变成自由连接链，折叠搜索空间会炸。所以真正要填的是**两个都保留一点刚度**那一格，
`ablate_backbone_terms.py` 只单变量扫过，缺了它。补上
（`scripts/sweep_angle_dihedral_scale.py`，16 个结构）：

**超帽比例（行 = angle 系数，列 = dihedral 系数）**

| | 0.003 | 0.01 | 0.03 | 0.1 | 0.3 | 1.0 |
|---|---|---|---|---|---|---|
| **0.003** | 1.5% | 2.2% | 4.4% | 11.6% | 19.0% | 24.4% |
| 0.01 | 1.7% | 2.1% | 4.6% | 11.7% | 18.8% | 24.4% |
| 0.03 | 2.1% | 2.7% | 5.0% | 11.9% | 18.9% | 24.4% |
| 0.1 | 3.3% | 4.1% | 7.1% | 13.4% | 19.0% | 24.4% |
| 0.3 | 7.1% | 8.0% | 10.4% | 14.8% | 19.6% | 24.6% |
| **1.0** | 12.9% | 13.6% | 15.0% | 17.8% | 21.5% | **25.3%** |

**dihedral 主导** —— 每列几乎不随 angle 变；要压到 2% 左右需要 dihedral ≤ 0.01。
p95 在 (0.003, 0.003) 处是 **130**，出厂值处是 **3675**。

**于是两条独立的线合上了。** §3s 用 `kBT/σ²` 算出的比例是
**angle 28.1/600 = 0.047**、**dihedral 7.2/500 = 0.014** ——
**正好落在这张网格的安全区（2.1–2.7% 超帽，p95 143–149）。**
方差判据和力网格，一个是统计的、一个是力学的，指向同一个值。

**建议的改动，两个数：`K_ANGLE` 600 → 28.1、`K_DIH` 500 → 7.2。**
链保住了弯曲刚度（非零），帽子从力律退化回兜底。

**三条保留意见：**

1. **漏斗那边是软证据。** 诱饵是高斯扰动珠子，而局部三项直接抓这个。
   `angle+dihedral off` 拿到 1.000 有很大一部分是因为**剩下的项本来就够判**，
   不是因为这两项被证明无用。真正可信的是力那一侧（27.1% → 1.9%）。
2. **井深只有几 kBT，链会变软。** §3s 已经指出：按 `kBT/σ²` 定的 dihedral 阱深只有 3.6 kBT，
   兜不住一个构象。**这是取舍，不是缺陷** —— 而这个取舍现在是**有力的证据支持的**：
   兜住它换来的是 26% 顶帽，而消融显示兜住它对漏斗没有好处。
3. **折叠就全部压在非键合项上了**（pair / bpp / stacking）。这在物理上是对的，
   但那些项我们一次都没验过。**这一条改变了下一步该做什么**：不是 IBI，
   是先看非键合项能不能承重。


### 3u. 非键合项能不能承重：能，而且配对项是唯一的承重者

§3t 把折叠全压到了非键合项上，而它们一次都没验过。高斯诱饵测不了这件事 ——
它会把键拉断、造出碰撞，所以任何惩罚局部几何的项都会抓到它，而配对项根本没被考到。

**换成长度自配对的诱饵（`scripts/test_nonbonded_fold_signal.py`）：同一个结构、同一批残基、
同一份几何，只把"谁和谁配"随机重排。几何一点没变，所以每一个键合项按构造都对差异免疫。**
如果依赖配对的项仍然把正确的那份排在更低，它们就带着折叠信息。

| 项 | 平均 delta | 支持天然 | 平均 \|E\| | 相对净能量 |
|---|---|---|---|---|
| **wc pair N-N** | **+29136.7** | **24/24** | 133.9 | 2.19% |
| **bpp** | **+9114.4** | **23/24** | 13767.9 | 225.57% |
| pair guide | +0.6 | 14/24 | 13.5 | 0.22% |
| bb bond P-P | 0.0 | 0/24 | 31.5 | 0.52% |
| intra P-C4' | 0.0 | 0/24 | 1.3 | 0.02% |
| intra C4'-N | 0.0 | 0/24 | 1.0 | 0.02% |
| angle P-P-P | 0.0 | 0/24 | 1907.6 | 31.25% |
| **dihedral P-P-P-P** | 0.0 | 0/24 | **6946.1** | **113.81%** |
| stacking P-P | 0.0 | 0/24 | 224.9 | 3.68% |
| bsj closure | 0.0 | 0/24 | 2637.7 | 43.22% |
| bsj guide | 0.0 | 0/24 | 10.7 | 0.17% |
| bsj contact | 0.0 | 0/24 | 314.6 | 5.15% |
| clash | 0.0 | 0/24 | 0.0 | 0.00% |
| **FULL（含 GB/SA）** | **+38213.2** | **23/24** | | |

**十一条几何项全部精确为 0.0** —— 这是负对照，确认测试确实只考了配对。
**而 FULL 的 +38,213 与 wc pair + bpp 的 +38,251 基本相等，GB/SA 贡献约 0。**

**结论一：配对项承重，而且承得住。** wc pair **24/24**、bpp **23/24**、总数 23/24。

**结论二：能量账本证实了 §3t 的判断。** dihedral 是**单项最大的能量项**（6,946，超过净能量），
angle 1,908，bsj closure 2,638 —— 而三个局部项加起来才 34。
**占着能量预算的两项做的是零折叠功。**

**三条保留：**

1. **诱饵太粗。** 随机重配对会让配对的伙伴隔得很远，配对能量被拉爆是**必然**的
   —— 和 §3k 的高斯诱饵是同一个毛病。24/24 说明的是"这些项对配对敏感、方向对"，
   **不是**"能把正确折叠从似是而非的错折叠里挑出来"。要问后者得用**近天然诱饵**
   （配准位移 register shift），那是下一个测试。
2. **一列标签曾经是错的。** 我先前把 `mean|delta| / mean|total|` 写成"占总能量比"，
   那是敏感度不是占比。已改成两列：真正的 `mean |E|` 和它相对净能量的比例。
   超过 100% 是因为总数是带符号的和、有大量抵消，它读作"该项能量量级相对净能量"。
3. **stacking 在这张表里是 0.0**，因为它是 P(i)-P(i+2) 距离项，而重配对不改几何。
   它是否承重，这个测试答不了 —— 要改几何才考得到它。


### 3v. 近天然诱饵：判别力只剩一项；以及两个常数落地

**配准位移诱饵（`scripts/test_register_shift_decoys.py`）。** 把每条螺旋的伙伴链平移一到两格 ——
残基还是那批，伙伴还在同一条螺旋里，几何上说得过去，只有配对关系错了。

| 项 | 平均 delta | 支持天然 |
|---|---|---|
| **wc pair N-N** | **−10.9** | **42/111** |
| **bpp** | **+16352.2** | **111/111** |
| pair guide | +5.6 | 57/111 |
| 其余十项 | 0.0 | 0/111 |
| **FULL（含 GB/SA）** | **+16819.0** | **111/111** |

**`wc pair` 掉到 42/111 —— 比抛硬币还差。全部判别力来自 bpp 一项。**

而且**这个诱饵没有我以为的那么近天然**：配准位移把伙伴推远了，P-P 从 1.658 ± 0.299 到
1.871 ± 0.136 nm。bpp 的形式是 `−K·softplus((1.0−r)/0.3)`，r 超过 1.0 就归零 —— **它本质是
距离判据，赢的可能是距离，不是配对关系。**

**还有一件更要紧的，我之前几轮都没说：我所有测试都传 `pair_w = torch.ones`。**
而这个力场**唯一的序列依赖就是 `pair_w`**。传全 1 之后它就是一个纯几何函数 ——
它**不可能**区分正确的 G-C 和错误的 G-G，只能靠距离。所以「能不能挑出似是而非的错折叠」
这个问题，在我测过的版本里，答案是**只能靠几何**。

对应地，§3u 那个 24/24 要重新读：随机重配对把伙伴甩到几纳米外，
`wc pair`（目标 1.0 nm，k=600）必然爆 —— 那是「对粗暴距离错误的反应」，不是「配对识别」。

**常数落地。** `K_ANGLE` 600 → **28.1**、`K_DIH` 500 → **7.2**（`kBT/σ²`）。
`_K_ANGLE`/`_K_DIH` 同步（死路径也曾因为快照和活路径分家，不留同一个坑）。
`tests/test_ff_bonded_targets.py` 加了两条断言把它们锁住。

**验证（`scripts/measure_bonded_force_distribution.py`，24 结构 2985 珠子）：**

| | 改前 | **改后** |
|---|---|---|
| 中位 \|F\| | 3.60 | 3.57 |
| p95 | 3506.53 | **166.65** |
| p99 | 10898.61 | **306.72** |
| max | 32753.82 | **520.51** |
| **超帽珠子** | **784 / 2985（26.26%）** | **97 / 2985（3.25%）** |

| 项 | p95 | max | 超帽 |
|---|---|---|---|
| bb bond P-P | 49.01 | 158.42 | 0 |
| intra P-C4' | 7.41 | 34.03 | 0 |
| intra C4'-N | 7.16 | 13.05 | 0 |
| **angle P-P-P** | **35.36** | **90.71** | **0** |
| **dihedral P-P-P-P** | 46.34 | **470.47** | **15** |
| **stacking P-P** | 90.88 | 429.10 | **46** |

**angle 项现在一次都不超帽。** 剩下超帽的 97 个珠子里，**stacking 占 46、dihedral 占 15**
—— **下一个候选是 stacking**，而它在 §3q/§3t 里已经被点过：P(i)-P(i+2) 距离该不该拿来代理 stacking。

**13 项逐项 gradcheck 在新常数下仍然全部自洽（最差 5.58e-07）。**

**而如实说：`tests/test_force_gradcheck.py` 还是红的。**
`cg_energy_forces` 的 max_rel 从 7.1708 到 5.8542，`cg_forces_explicit_batched` 从 4.2316 到 8.7794。
但**绝对误差腰斩**：1090.99 → 487.21。
超帽从 26% 掉到 3.25%，如果帽子是主因，max_rel 本该大幅下降 —— **它没有。
所以我那条「帽子导致 F ≠ -dE/dx」的解释仍然没有被确认**，只是绝对误差按预期降了。

### 3w. pair_w 那条门：关掉了，信号活着但弱了；以及冒烟测试

**管线里的 `pair_w` 是什么。** `isrnaclong.py:773` 把每一对的权重**整体替换成**
`compute_rcm_score(...)['confidence']`，丢掉前面在 738–760 行堆起来的 1.0 / 0.8 / 0.6 / BPP。
而 `rcm.py:221` 定义那个 confidence 为 `crossing / (crossing + within_up + within_down)` ——
分母含序列内部的重复，所以内部重复多的序列会给低分。

在训练集的真实序列上量（`scripts/measure_rcm_weight_range.py`，n=173）：

| 权重 | n | mean | sd | min | p25 | median | p75 | max |
|---|---|---|---|---|---|---|---|---|
| **真实序列** | 173 | 0.347 | 0.364 | 0.000 | 0.000 | **0.250** | 0.583 | 1.000 |
| 打乱对照 | 173 | 0.296 | 0.365 | 0.000 | 0.000 | 0.125 | 0.500 | 1.000 |

**44.5% 低于 0.20，41.0% 低于 0.10，39.3% 低于 0.05。**
也就是管线里约四成的配对约束是**关着的**。而打乱对照几乎分不开（0.347 对 0.296），
说明这个分数在这批数据上判别力很弱。

**所以 `pair_w = ones` 不是「把化学关掉」，是配对强度的上界。**
我在 §3v 里说反了一次，这里更正。

**信号活不活（`scripts/fold_signal_under_real_weights.py`，24 结构）：**

| 权重 | pair delta | pair 胜 | bpp delta | bpp 胜 | 合计 |
|---|---|---|---|---|---|
| ones（3u/3v 用的） | 30215.2 | 24/24 | 9581.7 | 22/24 | 23/24 |
| 全取中位 0.25 | 7458.8 | 24/24 | 2367.3 | 23/24 | 24/24 |
| **取自真实分布** | **10357.4** | **24/24** | **3202.9** | **22/24** | **22/24** |

**结论：§3u / §3v 的判断在管线的真实权重下成立，但弱了** —— 合计从 23–24/24 掉到 **22/24**，
`bpp` 的量级掉到三分之一。**不是虚假信号，但也不是满堂红。**

**顺手查出的第三个漂移 bug。** 第一版跑出来 `bpp` 的 delta 完全不随权重变（9581.7 → 9469.0），
那不对 —— bpp 能量正比于权重。查出来 `cg_force_terms.py` 里 bpp 漏了 `w`：
库里是 `-K_BPP * bpp_w * softplus(...)`（`torch_cgsim` 830–838 行），我那份副本没乘。
**这是同一个模块第三次和库漂移**（先是常数，再是 bsj contact 的力、再是它的能量，现在是 bpp 的权重）。
修了。§3u / §3v 的量级不受影响（那里权重全是 1，漏乘的因子就是 1），胜负计数也不受影响。

**冒烟测试（`scripts/smoke_cg_stage.py`）。** 直接驱动管线用的那个入口，
不是我的脚本：P-only 坐标（Å）加 `(i, j, w)` 三元组，就是 `isrnaclong.py:2040-2043` 传的形状。

```
structure 1ET4, L = 35, 15 WC pairs, sequence length 35
weights in the rough shape the RCM measurement gave: [0.0, 0.25, 0.8]
BatchedREMD2D constructed
run returned a 3-tuple
  coords shape (35, 3), dtype float32
  all finite: True
  energy: 5117.89306640625
  diag[acceptance_T] = [0.5]
  diag[acceptance_lam] = [1.0]
  diag[tri_force_injected] = False
```

**跑通了，不崩，输出有限，交换判据有值。** 而且顺手确认了一件事：`sequence` 在实践中是**必填**的
—— 省略它会在第一次 `run()` 里被 `real_cg_beads` 拒绝（那是我早先加的守卫，宁可报错也不生成假珠子）。
管线传了它（2030 行），所以管线没事。

**这一层到此能说的都说了。还没做的：** 真·端到端（`isrnaclong.py` 跑完整条 2013 nt）——
那要 ViennaRNA 的 PF 加 64 副本 REMD，不是这一轮的量。所以**「离接入还有多远」这句话我仍然没有测量**，
只能说：**我改的那一层，管线调的那个入口，带着真实形状的输入跑得通。**

### 3x. 力场现在长什么样（实测），以及我量错了一件事

**结构。** 每个核苷酸三个珠子：P、C4'、N9（嘌呤）/ N1（嘧啶）。十三条显式项加隐式溶剂，
除了 §3m/§3v 改的那四个常数，其余全部是手调的。

**当前常数（出货值）：**

```
K_BB 500    K_INTRA 400   K_PAIR 600    K_STACK 500
K_ANGLE 28.1   K_DIH 7.2   K_CLASH 500   K_BSJ 600
K_BSJ_GUIDE 100  K_PAIR_GUIDE 100  K_BSJ_CONTACT 50  K_BPP 600  K_MG 200

BOND_P_NEXT 0.5900  BOND_P_C4 0.3900  BOND_C4_N 0.3350
PAIR_NN 1.0000  STACK_R0 1.1250  CLASH_DIST 0.3000
ANGLE_PPP 150.00 deg (cos -0.866)   DIH_PPPP 12.84 deg (cos +0.975)
C_MG 0.01 M  C_NA 0.15 M
```

**实测（`scripts/describe_current_force_field.py`，20 结构，`pair_w = ones`）：**

| 项 | 平均 \|E\| | 占比 | p95 \|F\| | max \|F\| | 超帽 |
|---|---|---|---|---|---|
| **bpp** | **12554.0** | **117.7%** | **1195.95** | **2119.72** | **22.31%** |
| bsj closure | 2980.5 | 27.9% | 0.00 | **5249.57** | 1.18% |
| bsj contact | 311.5 | 2.9% | 4.13 | 4.71 | 0.00% |
| stacking P-P | 204.1 | 1.9% | 105.30 | 429.10 | 1.81% |
| wc pair N-N | 117.8 | 1.1% | 73.83 | 226.64 | 0.06% |
| dihedral P-P-P-P | 95.8 | 0.9% | 48.67 | 2479.10 | 0.53% |
| angle P-P-P | 81.2 | 0.8% | 41.97 | 93.04 | **0.00%** |
| bb bond P-P | 30.4 | 0.3% | 48.30 | 134.45 | 0.00% |
| pair guide | 13.0 | 0.1% | 35.08 | 450.80 | 1.09% |
| bsj guide | 12.1 | 0.1% | 0.00 | 442.58 | 0.06% |
| intra P-C4' / C4'-N | ≤1.3 | 0.0% | ≤8.25 | ≤29.21 | 0.00% |
| clash | 0.0 | 0.0% | 0.00 | 0.00 | 0.00% |

（用实测 RCM 权重时：bpp 4427.3 / 101.1%，超帽 13.02%。占比超 100% 是因为总数是带符号的和、
有大量抵消，它读作「该项能量量级相对净能量」。）

**结论一：我把 angle 和 dihedral 压下去之后，bpp 成了整个力场。**
单项占净能量的 100–118%，而 angle/dihedral/stacking 三项加起来只剩 3% 上下。
§3t 里我写过「折叠现在全压在非键合项上」—— 实测证实了，而且比那句话更极端：
**压在 bpp 一项上**，`wc pair` 只剩 0.9–1.1%。

**结论二，也是我要更正的一条：我量错了对象。**
我报的「超帽 26.26% → 3.25%」是在**六个键合项**的和上量的（`measure_bonded_force_distribution.py`），
**不含 bpp、pair guide、bsj、clash、GB**。而 bpp 单项就有 **22.31%** 的珠子超帽（真实权重下 13.02%）。
**所以整场的前后对比我没有做过** —— 我量的是子集，然后把子集的改善说成了力场的改善。
最初那次（`diagnose_cg_force_terms.py`，13 项）量的是全场，但那是改之前，而且只有 1EHZ 一个结构。
**要判断这次重定标对整场有没有用，需要一个包含全部项的前后对比。那个对比现在不存在。**

**结论三：还有两处没解释的。**

1. **`bsj closure` 有全场最大的单点力，5249.57 kJ/mol/nm。** 它只作用在两端两个珠子上，
   所以 p95 是 0.00 —— 但它是个闭环项，对这批**线性**测试结构本来就不该存在。
   在真实的环状构建上它是合法的，可它的大小需要单独看。
2. **`clash` 一次都没触发**（平均能量 0.0，力 0.0）。要么这批天然结构的碰撞阈值内没有对，
   要么这一项有问题。这两种可能我都没排除。

### 3y. 我改的是 GPU 版 —— 而这台机器不走它

**这一条比前面任何一条都实在，而且是我的疏忽。**

这个力场有**两份并行的实现**，各自带一套常数：

| | `torch_cgsim.py`（我改的） | `openmm_gpu_refiner.py`（没动） |
|---|---|---|
| K_PAIR | 600 | **1500** |
| **K_ANGLE** | **28.1** | **600** |
| **K_DIHEDRAL** | **7.2** | **800** |
| K_CLASH | 500 | **300** |
| K_BSJ | 600 | **800** |
| K_BSJ_GUIDE | 100 | **1200** |
| **DIH_PPPP** | **12.84°** | **33°** |
| **STACK_R0** | **1.125 nm，作用在 P(i)–P(i+2)** | **5.05 Å，作用在 N(i)–N(i+1)** |
| ANGLE_PPP | 150° | 150°（唯一一致的一项） |

**六个常数不一致。我改的四个里，三个在 OpenMM 那边还是旧值。**

**而 `openmm_gpu_refiner.py` 不是死代码**，它被至少六个模块引用：
`isrnaclong.py:1042` / `:1388`、**`rest2_remd_2d.py:231, 400`（CPU 回落）**、
`rest2_sampler.py:50, 222, 329`、`metadynamics_sampler.py:345`、`torch_gpu_refine.py:63, 89`。

**这台机器走哪条（实测）：**

```
torch 2.10.0+cpu
cuda available: False
device count: 0
```

`isrnaclong.py:2018` 是 `_use_gpu_rest2 = torch.cuda.is_available()`，所以 **Level 4 的 REST2 直接跳过
`BatchedREMD2D`，落到 2054 行的 CPU 回落** —— 而那条路 import 的是 `openmm_gpu_refiner`。

按层级看：

| 层级 | 走哪套力场 | 我的改动生效吗 |
|---|---|---|
| Level 1.5 全局弛豫 | `physical_relaxation`，自述「aligned with `openmm_gpu_refiner._build_3bead_system_gpu`」 | **不** |
| Level 2 refine | 优先 `torch_gpu_refine`，它用 `cg_energy_fores`（`torch_cgsim`） | **是**（前提是它 import 得成功） |
| Level 4 REST2 | CUDA 判断为假 → `rest2_remd_2d` → `openmm_gpu_refiner` | **不** |

**所以：这一整轮的四个常数、三个力 bug、那个快照，全部落在 `torch_cgsim.py` 里；
而在这台机器上，三个层级里有两个不走它。**

**我错在哪。** 上一轮回答「离接入还有多远」时，我读了 `isrnaclong.py:2024` 那一支就下了结论
「管线走的是活路径」，**但我读的是 GPU 那一支，也从没查过 `torch.cuda.is_available()` 在这台机器上是什么**。
两件事叠在一起才成了这个错：读对了一行，没读它的条件。

**这不代表改动没用。** 它们是关于 `torch_cgsim.py` 的、经过验证的事实；部署机（按记录是带 AMD GPU 的
AutoDL）上 CUDA/ROCm 可用时，Level 4 会走那条路。而且 `kBT/σ²` 那个判据是**关于「刚度该是多少」的陈述**，
它对 `openmm_gpu_refiner` 的 K_ANGLE 600 / K_DIHEDRAL 800 同样成立 —— 那两项按同一个判据也错 21 倍和 111 倍。

**但不能照抄。** 两边的二面角目标本来就不同（33° 对 180°），堆叠项作用在**不同原子对**上
（N(i)–N(i+1) 对 P(i)–P(i+2)），kinetic 常数也各自标定过。那是一次需要自己推导的移植，不是复制粘贴。

### 3z. 整场的前后对比（§3x 欠的那个）

`scripts/whole_field_before_after.py`：从 `70daa97`（力场改动前的最后一个 commit）取出
`torch_cgsim.py` 单独加载，和当前工作树在**同样的结构、同样的几何、同样的权重**上比整场。
帽子在两个版本里都有，所以可比的统计量是**顶在帽子上的珠子数** —— 那计的是原始力超过 200 的珠子。

| | mean \|F\| | p95 | max | 顶帽珠子 | 比例 |
|---|---|---|---|---|---|
| **`pair_w = ones`** | | | | | |
| 改前（`70daa97`） | 111.75 | 200.00 | 200.00 | 1726 | **53.77%** |
| 改后（工作树） | 80.86 | 200.00 | 200.00 | 913 | **28.44%** |
| **真实 RCM 权重** | | | | | |
| 改前 | 94.16 | 200.00 | 200.00 | 1420 | **44.24%** |
| 改后 | 63.19 | 200.00 | 200.00 | 615 | **19.16%** |

**结论一：整场确实变好了，大致减半。** 53.77% → 28.44%，44.24% → 19.16%。

**结论二：但帽子仍然是力律。** 四种情形下 p95 都是 **200.00** —— 意思是总有超过 5% 的珠子
顶在帽上。而且 **28% 的珠子精确落在帽子上**，不是我那个子集数字（3.25%）说的意思。
**子集的数字 understate 了大约九倍**，因为 bpp 被排除在外。

**结论三：而现在顶着帽子的是 bpp。** §3x 量过，改后这 28.44% 里 bpp 单项贡献 **22.31 个百分点**。

**bpp 为什么必然顶帽，不是调参能解决的。**

它的形式是 `E = -K_BPP * w * softplus((1.0 - r) / 0.3)`，力是
`K_BPP / 0.3 * w * sigmoid(x)`。在目标距离 r = 1.0 nm 处 x = 0，sigmoid(0) = 0.5，
所以**每一对在它该在的位置上，这一项就施加**

```
600 / 0.3 * 0.5 = 1000 kJ/mol/nm
```

**—— 帽子的五倍，与几何无关，恒成立。** 一个在目标位置就施五倍上限力的项，没法处于平衡；
它只会把所有配对往里拉，直到别的项（碰撞、堆叠）把它顶住。
softplus 还是**单边**的：r 大于 1.0 之后力趋近于零，所以它没有回复分支，永远不推。

要让它落在帽子下（w = 1 时）：`K_BPP / 0.3 * 0.5 <= 200`，即 **K_BPP ≤ 120**。
而多个对同时作用在一个珠子上，所以实际还要更低。

**这是下一步，而且它有和上一次一样的形状**：一个从数据来的判据、一张网格、一个前后对比。
不做之前我不知道该取多少。

### 3aa. bpp 定标：判据与网格第二次合上

**判据。** `kBT/σ²` 对 bpp 不成立 —— softplus 是单边的、没有平衡位置，没有曲率可配。
用的是一条力学的界，但它经由数据：**两个约束同一个量的项，力的量级不该差五十倍。**

谐振的 `wc pair N-N` 在观测几何下的力尺度是 `kBT/σ_NN`；bpp 在目标距离处的力是 `K_BPP / 0.6`
（`E = -K_BPP*w*softplus((1-r)/0.3)`，`x=0` 处 sigmoid(0)=0.5）。令两者相等：

```
K_BPP = 0.6 * kBT / sigma_NN
```

实测（561 对，20 结构）：`σ_NN = 0.112 nm`，于是 **K_BPP = 13.4**。出货值是 600，
它在目标距离处施加 **1000 kJ/mol/nm —— 帽子的五倍，与几何无关**。

**网格（`scripts/calibrate_bpp.py`）：**

| K_BPP | force@r0 | 顶帽（ones） | 顶帽（RCM） | 配准位移胜 |
|---|---|---|---|---|
| 3.0 | 5 | 6.48% | 6.39% | **8/16** |
| **10.0** | 17 | 6.48% | 6.39% | **16/16** |
| 30.0 | 50 | 6.60% | 6.42% | 16/16 |
| 100.0 | 167 | 8.50% | 6.60% | 16/16 |
| 300.0 | 500 | **29.13%** | **15.81%** | 16/16 |
| 600.0（出货） | 1000 | **29.19%** | **19.27%** | 16/16 |

三件事：

1. **100 到 300 之间有一道陡坎** —— 300 以上顶帽跳到 29%，100 以下只有 6.5–8.5%。
   **顶帽完全由 bpp 在大值区驱动。**
2. **折叠信号在 10 到 600 之间平的是 16/16**，只有掉到 3.0 才崩（8/16）。
   bpp 不是在做细粒度判别，它**近乎是个开关** —— 这与 softplus 的单边饱和形式一致。
3. 判据 13.4 落在安全区且信号满。**但信号从 3 到 10 是悬崖，13.4 就在崖边上方，余量不大。**

**落地：`K_BPP` 600 → 13.4**，`_K_BPP` 同步，测试锁住。

**整场前后对比（`scripts/whole_field_before_after.py`，对 `70daa97`）：**

| | mean \|F\| | 顶帽珠子 | 比例 |
|---|---|---|---|
| 改前 ones | 111.75 | 1726 | **53.77%** |
| 改后 ones | 47.88 | 199 | **6.20%** |
| 改前 RCM | 94.16 | 1420 | **44.24%** |
| 改后 RCM | 40.46 | 197 | **6.14%** |

**53.77% → 6.20%，44.24% → 6.14%。** 对比 §3z 的 28.44% / 19.16%，
**bpp 这一步把剩下的顶帽拿掉了四分之三。**

**bpp 自己的超帽：22.31% → 0.00%。**

**当前账本（`scripts/describe_current_force_field.py`）：**

| 项 | 平均 \|E\| | 占比 | p95 \|F\| | max \|F\| | 超帽 |
|---|---|---|---|---|---|
| **bsj closure** | **2980.5** | **90.7%** | 0.00 | **5249.57** | 1.18% |
| bsj contact | 311.5 | 9.5% | 4.13 | 4.71 | 0.00% |
| bpp | 280.4 | 8.5% | 26.71 | 47.34 | **0.00%** |
| stacking P-P | 204.1 | 6.2% | 105.30 | 429.10 | **1.81%** |
| wc pair N-N | 117.8 | 3.6% | 73.83 | 226.64 | 0.06% |
| dihedral P-P-P-P | 95.8 | 2.9% | 48.67 | 2479.10 | 0.53% |
| angle P-P-P | 81.2 | 2.5% | 41.97 | 93.04 | **0.00%** |
| bb bond P-P | 30.4 | 0.9% | 48.30 | 134.45 | 0.00% |
| pair guide | 13.0 | 0.4% | 35.08 | 450.80 | 1.09% |
| bsj guide | 12.1 | 0.4% | 0.00 | 442.58 | 0.06% |
| intra P-C4' / C4'-N | ≤1.3 | 0.0% | ≤8.25 | ≤29.21 | 0.00% |
| clash | 0.0 | 0.0% | 0.00 | 0.00 | 0.00% |

**而这里有个必须指出的问题：`bsj closure` 现在看着最大（90.7%），但它是闭环项，
而这批测试结构是线性的。** 它不该在这里作用 —— 那 90.7% 是**测试工况的产物，不是力场的性质**。
真实的构建是环状的，那里它是合法的。**但这意味着我所有的「整场」数字都混进了一个在测试几何上
不该存在的项**，这一点之前每一节都没说。要干净地量整场，需要一个环状的测试结构 —— 现在没有。

**剩下顶着帽的（6.2% 里）：** stacking 1.81%、bsj guide 0.06%、pair guide 1.09%、dihedral 0.53%、
bsj closure 1.18%。**下一个候选是 stacking**，而它的合理性从 §3q 起就被质疑过。

**pytest：2 failed, 10 passed。** `cg_energy_forces` 的 max_rel 5.8542 → **3.0866**
（max_abs 487.21 → 435.51）。每改一次都在降，但仍然不过，
**所以「帽子导致 F ≠ -dE/dx」这条解释依然没有被确认。**

### 3ab. 环状测试结构：我试了，失败了，而失败的方式是老毛病

§3aa 说所有「整场」数字都含 `bsj closure`，而测试结构是线性的，所以要先补一个环状结构。

**我的做法**：一条环状 RNA 就是首尾骨架相邻的线性链，所以把每个天然结构**按残基编号循环平移** ——
原来的 `k` 号变 `0` 号，原来的 `k−1` 号变 `L−1` 号，配对表同步。这样 `P(0)` 和 `P(L−1)` 本就是相邻残基，
闭环约束被满足。

**结果：**

| | 线性 | 环状重编号 |
|---|---|---|
| **bsj closure** | 2980.5，max **5249.57**，超帽 1.18% | **0.6，max 58.42，超帽 0.00%** |
| stacking P-P | 204.1，max 429.10 | **3784.3，max 4206.36** |
| bb bond P-P | 30.4，max 134.45 | **2513.7，max 4380.69** |
| bsj guide | 12.1 | **219.3** |
| 整场 mean \|F\| | 40.48 | 43.44 |
| **整场超帽** | **6.14%** | **7.94%** |

**闭环项确实归零了。但 `bb bond` 从 30.4 涨到 2513.7，`stacking` 从 204.1 涨到 3784.3。**

**原因**：循环平移把残基顺序变成 `k…L−1, 0…k−1`，于是新编号里 `L−1` 与 `0` 之间那根「键」，
是原链两端之间的那一根 —— 而它们平均相隔 5.012 nm。**我凭空造了一根键。**

**这正是我早先在加载器里修过的那类错误**（`boltzmann_bonded._chain_residues` 把跨残基间隙的两项
当成骨架键），同一个病：**把两个在空间上不相邻的残基当成相邻的。** 那次是无意的，这次是我自己干的。

**一个线性结构不是环，改编号不能把它变成环。** 要有环状测试结构，得真的把一条序列折成一个闭环，
而那需要一次折叠 —— 现在没有。

**所以退回到诚实的做法：BSJ 三项在测试几何上无法评估，把它们的贡献作为上界报出来。**

在线性结构上（§3aa 的表）：

- 整场超帽 **6.14%**
- 各单项自己的超帽：stacking **1.81%**、bsj closure **1.18%**、pair guide **1.09%**、
  dihedral 0.53%、bsj guide 0.06%

**BSJ 三项自身超帽之和是 1.24 个百分点，所以去掉它们在测试几何上的假象后，
整场的真实超帽上界是 6.14%，下界约 5%。** 精确值给不了 —— 帽子是在总力向量上施加的，
没法在事后把某一项减掉。

**结论：`bsj closure` 那 90.7% 的能量占比确实是工况产物，这一点坐实了；
但它对整场超帽的贡献只有 1.18 个百分点 —— 也就是说 §3aa 里那个 6.20% 的改善并不是靠它撑起来的。**
线性结构上真正顶着帽子的是 **stacking**。

### 3ac. stacking：一个精确冗余的项，已关掉

它在超帽上占 1.81%（§3ab），在能量上占 6.2%，而它的合理性从 §3q 起就被质疑。

**先验一条恒等式。** 记 `b_i = P(i+1)−P(i)`、`b_{i+1} = P(i+2)−P(i+1)`，
`cos_a` 是本文件定义的 P-P-P 余弦，则

```
|P(i) − P(i+2)|² = |b_i|² + |b_{i+1}|² − 2|b_i||b_{i+1}|·cos_a
```

**这是精确的。** `scripts/assess_stacking_redundancy.py` 在 1278 个窗口上验：

| | |
|---|---|
| 直接量的 `\|P(i)−P(i+2)\|²` 均值 | 1.208426 |
| 由两个键与 `cos_a` 算的均值 | 1.208426 |
| 最大绝对差 | **6.661e-16 nm²** |
| 最大相对差 | **8.515e-16** |
| `\|P(i)−P(i+2)\|` 由键与角预测的 R² | **1.000000** |

**两个键长由 `K_BB` 约束，`cos_a` 由 `K_ANGLE` 约束。所以这一项是第三个弹簧，压在一个导出量上。**

**而且这个模型根本表达不了 stacking。** 珠子是 P、C4'、和一个点状的 N9/N1 —— 没有平面、没有法向、
没有 rise 和 twist。**一个以 stacking 为名、约束一个骨架距离的项，从来没在做那件事。**

**消融（`scripts/assess_stacking_redundancy.py`）：**

| | rank | gap | p95 \|F\| | 超帽 |
|---|---|---|---|---|
| stacking ×1（出货） | 1.000 | 0.0 | **181.68** | **4.02%** |
| stacking ×0.1 | 1.000 | 0.0 | 101.08 | **0.55%** |
| **stacking off** | **1.000** | **0.0** | **94.64** | **0.58%** |

**有没有它，rank 和 gap 一模一样。** 而关掉之后超帽从 4.02% 掉到 0.58%。

**唯一能救它的可能，我测了，答案是不。** 它也许在替某个约束得太松的项补漏 ——
候选是键项，§3s 的判据说它该是 `kBT/σ² = 1076` 而出货是 500。

| K_BB | stacking | rank | gap | p95 \|F\| | 超帽 |
|---|---|---|---|---|---|
| 500.0（出货） | on | 1.000 | 0.0 | 181.68 | **4.02%** |
| **500.0** | **off** | **1.000** | **0.0** | **94.64** | **0.58%** |
| 1076.2（kBT/σ²） | on | 1.000 | 0.0 | 215.96 | 6.26% |
| 1076.2 | off | 1.000 | 0.0 | 138.48 | 1.61% |

**键硬了之后超帽升，不是降。它不是在补漏。**

**而顺带测出一条：`kBT/σ²` 这个判据对键项不成立。** 它把超帽从 0.58% 推到 1.61%。
和 §3s 同一个教训 —— **匹配方差 ≠ 对非高斯分布有好处的力**。键项保持 500，测试里锁住。

**落地：`K_STACK` 500 → 0.0**，`_K_STACK` 同步，`STACK_R0` 保留（它记录那个坐标的众数，
而恒等式说这个众数现在由键项和角项共同给出）。

**整场前后对比（对 `70daa97`）：**

| | mean \|F\| | p95 | max | 顶帽珠子 | 比例 |
|---|---|---|---|---|---|
| 改前 ones | 111.75 | 200.00 | 200.00 | 1726 | **53.77%** |
| 改后 ones | 36.79 | **149.86** | 200.00 | 99 | **3.08%** |
| 改前 RCM | 94.16 | 200.00 | 200.00 | 1420 | **44.24%** |
| 改后 RCM | 29.37 | **141.27** | 200.00 | 97 | **3.02%** |

**而 p95 从 200.00 掉到 149.86 / 141.27 —— 落在帽子下面了。这一步的意思是：帽子不再是力律。**
前面几节里 p95 一直是 200.00，意思是总有超过 5% 的珠子顶在帽上；现在不是了。

**测试：12 passed, 2 failed**，两条失败仍是既有的 `test_force_gradcheck.py`。
另外我那条 `test_both_paths_read_the_live_stacking_target` 一开始挂了 —— 挂得对，
`K_STACK = 0` 之后那一项没有能量可变。改成临时把它打开再验，保住了那条测试的检出能力。

### 3ad. 三件：clash 的三个缺陷、gradcheck 的真相、以及统计势到底在哪

## 一、clash 从来不触发：三个缺陷，都修了

**先答「为什么」。** 24 个天然结构、3978 个珠子，最近的一对珠子距离**最小 0.309 nm，中位 0.379，最大 0.445**。
阈值 `CLASH_DIST = 0.30`。**0 / 24 在阈值内。** 所以这一项**是对的，只是没活干** —— 
天然 RNA 里没有两个珠子相隔 3 Å。

**但最接近的那对离阈值只有 0.009 nm —— 这是一道刀刃。** 骨架稍微压一下就会以 `K_CLASH = 500` 猛力反弹。
这一点值得单独看。

**而真正的缺陷是三个，全是静默的：**

1. **`GPUCellList.build` 用 `pos_nm[0]` 建表**（1176 行）。一个在副本 0 里相距 6 nm、在副本 5 里重叠的配对，
   **根本不在表里**。管线跑 64 个副本 —— **63 个没有排除体积**。
2. **`build` 只在同一个 cell 内枚举配对**，不查相邻 27 格。跨格边界的近对**即使在副本 0 也会被漏掉**。
   原来 `cell_size = 1.5`，而 `close = dist < cs*2.0` 假设了 2 格，枚举却只做了 1 格。
3. **`_clash_f` 的掩码是 `dist[0] < r_cut`** —— 副本 0 决定所有副本哪些对算碰撞。
   `torch_cgsim.py:1379` 那份还带着注释自己写着 `# (M,) judge using only the first batch`。

**修法**：邻居表按**所有副本取并集**，搜索格取 `min(cell_size, CLASH_DIST)`，
这样 27 格邻域覆盖 3×CLASH_DIST，必然包含所有可能落在阈值内的对，而配对数还压得住；
掩码改成逐副本、向量化。

**验证（`scripts/diagnose_clash_mask.py`）：**

| | 修前 | 修后 |
|---|---|---|
| 副本 1 压到 0.05 nm，副本 0 留在 6.0 nm | `[0., 0.]`，**该对不在表里** | **`[0., 15.625]`** |
| 副本 0 压到 0.05 nm | `[15.625, 0.]` | `[15.625, 0.]` |

`0.5 × 500 × (0.3 − 0.05)² = 15.625` ✓。**副本 1 的严重碰撞以前完全不可见。**

整场数字没变（3.08% / 3.02%），因为天然结构上本来就没触发。

## 二、gradcheck：统计量是错的，而势是不连续的

**两层原因，都不是「某个力算错了」。**

**第一层：那个统计量不能用。** 原版报的是六个探针里
`|F_解析 − F_有限差分| / (|某个力分量| + 1e-6)` 的最大值，**而分母可以穿过零**，
比值自己就会炸。它在整条弧线上给出过 11.42 / 7.17 / 5.85 / 3.09 / 6.51 —— **没有趋势，因为没有什么在趋势**。
`scripts/diagnose_cap_gradcheck.py` 抓到它**精确返回 1.0000**（意味着有限差分是 0）。

**第二层：整条路径的能量本来就不连续。** 换成标准的相对 L2 范数之后失败在 1.07 —— 那比梯度本身还大，不是噪声。
而**逐项的十三个解析项是自洽的**（相对 L2 0.0036）。差异全部在 GB/SA 那一块：

| 量 | 相对 L2 | max abs |
|---|---|---|
| 十三个解析项 | 0.0036 | 10.42 |
| 整条路径（帽关） | **1.0737** | **3750.43** |
| GB/SA + Manning（差出来） | **1.0007** | **3750.43** |

**而 GB 那块的力本身最大只有 2.8 kJ/mol/nm，能量只有 −7.29 kJ/mol。能量差 7.29、梯度差 3750 —— 那只可能是能量有台阶。**

`scripts/diagnose_energy_discontinuity.py` 直接验（移动珠子 0 一个轴 1e-5 nm）：

| | E(+h) − E | E(−h) − E |
|---|---|---|
| 十三项 | −0.024784 | **+0.024784** ← 反对称 |
| **整条路径** | −0.024414 | **−0.049805** ← 两边都往下 |

GB 那部分在 pos 与 +h 处都是 −7.292，在 **−h 处是 −7.366**。**跳了 0.074 kJ/mol。**
而 `2.0 × exp(−1.0/0.304)/1.0 = 0.074` —— **正好是一对配对跨过 1.0 nm 的 cell 过滤器进/出。**

**根源：`cg_energy_forces` 每次调用都从当前坐标重建 GB/SA 的配对表（硬 cell 过滤，无切换函数）。**
同一次调用里表和梯度自洽，**但势本身有台阶**。台阶除以 2h 就是那个巨大的「有限差分力」。

**所以整条路径在原理上就通不过任何 h 的有限差分检查 —— 断言它等于断言一件不可能的事。**

**在真实尺度上这个台阶有多大（`scripts/measure_gb_discontinuity.py`，8 个结构，位移 1e-4 nm）：**

| | |
|---|---|
| 中位跳变 | **0.000046 kJ/mol（0.0000 kBT）** |
| 最大跳变 | **0.001953 kJ/mol（0.0008 kBT）** |

**在真实密度下可以忽略。** 我那条测试链是直线，很多对挤在边界上，那里是 0.074。
**但只动了一个珠子；一次积分步里所有珠子都动，跨界的对数会更多** —— 这一点我没有量。

**测试改成测它能测的东西（`tests/test_force_gradcheck.py`）：**

1. 十三个解析项对各自的能量梯度（相对 L2 范数，< 0.02）
2. 力帽本身的行为 —— 它是施加在输出上的约束，单独一条测试，不再藏在一致性检查里
3. **GB 的不连续性作为一条实测事实写进测试** —— 将来若加了切换函数，它就该失败，
   提示把它换成一条真正的整路径 gradcheck

**并且 `cg_energy_forces` 新增了 `force_cap=None`。** 那不是调参旋钮：
**重缩放总力向量是对输出的非线性操作、不是能量里的一项，所以它一触发，返回的力就按构造不是 `−dE/dx`，
任何逐项检查在那里都通不过。** 有了这个参数，逐项一致性和力帽才能分开测，而不是互相掩盖。

**结果：15 passed。**

## 三、统计势到底在哪：不在力场里

`src/` 里对 `cg_rnasp` / `cgRNASP` / `boltzmann` 的引用**是零个**（grep 实测）。具体：

| | 状态 |
|---|---|
| cgRNASP（Tan 组的三珠统计势） | 只在 `scripts/cg_rnasp_evaluator.py` 里求值，**从未接进库里** |
| DBI 玻尔兹曼表（`scripts/boltzmann_bonded.py`） | 同样只在脚本里 |
| TriRNASP（`trirnasp_*.py`） | 模块在，但 `isrnaclong.py` 把 `use_trirnasp=False`、`use_trirnasp_force=False` —— **惰性** |
| **力场里实际有的** | **全部是手调的谐振项 + GB/SA，一个统计势都没有** |

**但统计势确实改了这个力场 —— 不是作为一项加进去，而是作为目标值和刚度的来源：**

| 来源 | 给出的东西 |
|---|---|
| 191 / 96 个沉积结构的分布众数 | `STACK_R0` 1.125 nm、二面角目标 cos +0.975（§3m） |
| `kBT/σ²` | `K_ANGLE` 28.1、`K_DIH` 7.2（§3s） |
| `0.6·kBT/σ_NN` | `K_BPP` 13.4（§3aa） |
| 恒等式 + 消融 | `K_STACK` → 0（§3ac） |

**四个被重定标的常数全部来自对实验结构数据库的统计。这就是统计势在这个项目里的用法：
它定的是「目标在哪、该多硬」，不是「再叠一个能量项」。**
而这恰好是 §3n 从综述里抄下来的那条：**拿实验分布建的 CG 势本来就叫统计势**，
而 DBI 是它的标准构造。

**没有做成的那一半是 IBI。** §3s/§3p 的结论是标度必须由迭代定（让模拟分布复现参考分布），
而 §3s 同时发现池化的数据库 σ 不是任何单态的宽度 —— 所以那一步不是可选项。现在没有做。

### 3ae. GB 台阶在真实步长下有多大：收尾，以及我在这条里犯的符号错误

§3ad 量的是**移动一个珠子 1e-4 nm** 的台阶，并注明「一次积分步里所有珠子都动，这一点没有量」。补上了。

**方法**：对**所有**珠子同时施加位移 u，则 `E(+u) − E(−u) = 2∇E·u + O(δ³) = −2F·u + O(δ³)`，
残余即是台阶。

**我的错**：第一版写的是 `s_term = 2*(F·u)` 然后算 `(E(+u)−E(−u)) − s_term` —— **符号反了**，
所以量到的是 `4|F·u|`。症状很明显：**残余随 δ 线性增长**（δ 涨 10 倍残余涨 10 倍），
而一个不连续项不该随 δ 缩放；而且它恰好等于光滑列的 2 倍。修了。

**修正后（6 结构，每档 12 个随机方向）：**

| 结构 | 步长 nm | 光滑项 \|2F·u\| | 台阶中位 | 台阶最大 | max/kBT |
|---|---|---|---|---|---|
| 1ET4 | 1e-4 | 0.2615 | 0.0128 | 0.0291 | 0.012 |
| 1ET4 | 1e-3 | 2.3833 | 0.0843 | 0.7626 | 0.31 |
| 1ET4 | 1e-2 | 13.0947 | 2.2746 | 5.3992 | 2.16 |
| 1KXK | 1e-2 | 13.9946 | 1.8842 | 6.1120 | 2.45 |
| 1L2X | 1e-2 | 8.4086 | 3.7639 | **9.6320** | **3.86** |
| 1NBS | 1e-2 | 24.2968 | 0.4505 | 3.9166 | 1.57 |

**真实步长是多少（读出来的，不是算的）：** `batch_langevin_step` 的 `dt_ps = 0.002` —— **2 fs**。
300 K 下 100 Da 珠子的热速度 `sqrt(3kBT/m) = 273 m/s`，**每步位移约 5e-4 nm**。

**所以落在 1e-4 与 1e-3 之间：台阶 0.029 – 0.78 kJ/mol，即 0.012 – 0.31 kBT。**

**结论：真实步长下它是 0.01–0.3 kBT 量级 —— 小，但对 REMD 的 Metropolis 判据不是零。**
而且残余**仍随 δ 增长**（不是 O(δ⁰)），说明里面还混着光滑的高阶项，不是纯不连续 ——
所以这个数字读作「能量变化里梯度解释不了的部分」，不是纯台阶大小。**这一点我没有拆开。**

**要把它真的削掉，需要在 1.0 nm 的 cell 过滤器上加一个切换函数。现在没有加。**
理由是它的量级（≤0.3 kBT/步）不足以先于下面那件更值得做的事。

### 3af. 统计势给了常数，但没给序列依赖 —— 以及池化到底扔掉了什么（实测）

**问题**：力场是被统计势改过的，那它不该与序列有关吗。

**答**：改的是**目标值和刚度**，不是按碱基发牌。一个统计势只有当直方图**按碱基身份分层**时才带序列
（cgRNASP 的表就是按碱基对型索引的）；我们的是池化的，所以它带的是**序列平均**，正好相反。

代码上：

- `boltzmann_bonded.fit()` 一行 `v = np.concatenate(vals)` 把全部结构的全部观测收进一个数组，
  随后一次 `np.histogram`。`coords_of` 根本不接受任何序列参数。
- 该文件里读 `rname` 只有两处，都是解析必需：`gly = "N9" if rname in ("A","G") else "N1"`
  （选珠子名，腺嘌呤的糖苷氮叫 N9、嘧啶的叫 N1）、以及 `WCP`（判哪些对算 WC 对）。
  **两处都不进直方图。**
- `torch_cgsim.py` 里 `resname|res_name|residue_type|base_type` **零匹配**（grep 实测），
  `cg_energy_forces` 的签名里没有序列。

**实测 A：碱基身份能解释多少方差。**（`scripts/test_sequence_dependence.py`，126 条链 / 191 个文件）

| 坐标 | 分层方式 | n | η² | 打乱标签 | 比值 |
|---|---|---|---|---|---|
| bb_bond | 碱基步 i,i+1 | 6638 | 0.0149 | 0.0021 | 7.2 |
| intra_pc | 碱基 i | 6764 | 0.0049 | 0.0005 | 10.1 |
| intra_cn | 碱基 i | 6764 | **0.0401** | 0.0004 | **107.1** |
| angle | 三联体 | 6512 | 0.0479 | 0.0101 | 4.7 |
| angle | 中间碱基 i+1 | 6512 | 0.0042 | 0.0005 | 8.4 |
| dihedral | 中央步 | 6386 | 0.0304 | 0.0022 | 13.6 |
| stack | 碱基步 i,i+2 | 6512 | 0.0144 | 0.0023 | 6.3 |

**0.5–4.8%，比值 4.7–107× —— 信号是真的，但小。** 最大的一项是 `intra_cn`（C4'–N）4.0%，
化学上应当如此：嘌呤是 C–N9、嘧啶是 C–N1，池化把两者平均了。这也说明池化**确实**扔掉了东西，
只是扔得不多。

**实测 B：把 `pair_w` 归零，几何和配对完全不动。**（10 个结构）

| 项 | ΔE 合计 | 移动的力分量 |
|---|---|---|
| bpp | 2354.666579 | 933 |
| wc pair N-N | 887.584615 | 933 |
| 其余十项 | **0.000000** | **0** |

所以力场带序列的入口只有两个：`pairs_ij`（哪些对存在）和 `pair_w`（它们的权重）。
两个都在 `isrnaclong.py` 里建（:738-760，然后 :773 被 RCM confidence 覆盖），
**都不是统计势给的**。统计势只给过常数。顺带确认 `pair guide` 不动 —— 它没有 `w`（代码 843-847 行）。

**实测 C：池化扔掉的那一点不是零。**

| 对型 | n | mean | σ | 若按此 σ 的 K_BPP |
|---|---|---|---|---|
| A·U | 936 | 0.9288 | 0.1154 | 13.0 |
| G·C | 1520 | 0.9405 | 0.0925 | 16.2 |
| **G·U** | 603 | **1.0019** | **0.1260** | **11.9** |
| 池化 | 3059 | 0.9490 | 0.1103 | 13.6（库里 13.4） |

σ_between = 0.0267、σ_within = 0.1071 —— 对型解释 N–N 方差的 **5.9%**。
最大的均值位移是 G·U − A·U = **0.0731 nm = 0.66 个池化 σ**。
**G·U 摆动对确实更宽**，而平铺的 K_BPP 13.4 把它们当成和 A·U 一样。

**所以正确的说法**：这个力场与序列有关，但只通过一条弱通道（RCM confidence，实测中位 0.250、
41% 低于 0.10）。而它弱的原因和上面是**同一个设计选择** —— 场内没有任何一项按碱基分型，
所以没有分型化学可供那个分数去比。

**修法（未做）**：把六张表按碱基身份分层。4 组（碱基）数据量够（每坐标约 6500 个观测）；
16 组（碱基步）勉强。最便宜的第一步是给 `K_BPP` 按对型分型（三档 13.0 / 16.2 / 11.9）。

### 3ag. IBI 的第一步：靶子站得住，而它立刻给出两个定论（实测）

IBI 需要一个"系统自己的平衡分布"当靶子。我们的靶子是 191 个沉积结构池化出来的，
不是任何单体的平衡分布，所以先要问它还能不能用。

**一、方差分解。**（`scripts/decompose_database_variance.py`，126 条链）

| 坐标 | σ_pool | σ_between | σ_within | between 占比 |
|---|---|---|---|---|
| bb_bond | 0.0477 | 0.0076 | 0.0471 | 0.026 |
| intra_pc | 0.0107 | 0.0027 | 0.0103 | 0.063 |
| intra_cn | 0.0083 | 0.0027 | 0.0078 | 0.105 |
| angle | 0.3070 | 0.0681 | 0.2994 | 0.049 |
| dihedral | 0.6026 | 0.1363 | 0.5870 | 0.051 |
| stack | 0.1225 | 0.0236 | 0.1202 | 0.037 |

多 MODEL 文件 **0 个**，所以库里没有 NMR 系综可用。

**§3s 那句话对了一半。** 我说池化的 σ "混了不同构象态，不是任何单态的宽度" ——
方向对，机制错：跨分子的偏移只占 **2.6–10.5%**，其余全是**同一条链内部逐残基**的差异。
两者都不是一个自由度的热宽度，所以结论不变。
**但由此得到一个有利的推论：池化分布 ≈ 单链自身的分布**，所以单链采样可以当靶子的替身 ——
那 §3s 的失败就不是靶子的错，是**去掉非键合项**的错（只有键合项的链没有理由保持折叠）。

**二、三个刚坐标是解析的，不需要模拟。**

C4' 只出现在三处：`intra P-C4'`、`intra C4'-N`、clash。而 clash 一次都不触发
（最近一对 0.309 nm vs 阈值 0.300）。所以 P–C4' 实质上**一维**，
且 `_bond_f` 是 `0.5*k*(r-r0)^2`（618–628 行），于是热宽度精确等于 `sqrt(kBT/k)`：

| 坐标 | 出厂 k | 解析 σ_1D | 参考 σ | IBI 给出的 k | 出厂差 |
|---|---|---|---|---|---|
| intra_pc | 400 | **0.0790** | 0.0110 | 20612 | **51.6× 太软** |
| intra_cn | 400 | **0.0790** | 0.0083 | 36198 | **90.5× 太软** |
| bb_bond | 500 | 0.0706 | 0.0470 | 1129.2 | 2.26× 太软 |

**两个 intra 键软了 52 倍和 90 倍，而不采样看不出来。** 我此前所有检查都测不到它 ——
力帽、展开信号、常数锁都不行 —— 因为出厂值在天然几何附近给的力只有 `k*0.011 ≈ 4` kJ/mol/nm，
离 200 的帽子差两个数量级。它只在**宽度**上现形。

注意这是**解析结论，不是迭代出来的**：坐标既然是一维，IBI 第一轮就会精确落到
`kBT/σ_ref²`，也就是 §3s 那一列的值（20752.7 / 36399.2，与上表一致到 1%）。

`bb_bond` 只差 2.26 倍，而 §3z 已经测过：照同一个 `kBT/σ²` 判据调硬，超帽会从 0.58% 涨到 1.61%。
**所以三个局部项里，两个必须改（差两个数量级），第三个不能照同一判据改。**

一句保留：上面假定 clash 始终不触发。跑长了如果有珠子逼近 0.30 nm，这个解析结论会被污染，
所以长跑里要顺便盯 clash 的激活次数。

**三、angle / dihedral / stack 是耦合的，耦合修正是 IBI 唯一真正的内容。**
判据不是采样 σ 对参考 σ，而是**采样 σ 对它自己的一维预测 `sqrt(kBT/k)`** ——
出厂 k 本来就是照 `kBT/σ_ref²` 定的，所以 `sim/1D` 里剩下的恰好是耦合。
`scripts/ibi_round0.py` 跑了 32 副本 × 120 ps 的全场 Langevin，结果是
bb_bond 2.785、angle 1.857、dihedral 1.061、stack 对参考 2.843。
**但这些数不能用 —— 采样器本身是错的，见 §3ah。** 先修工具，这一节才谈得上结论。

### 3ah. 恒温器是错的：GPU 路径的有效温度是 0.4T（实测 + 解析）

**起因是好的：** §3ag 里 bb_bond 的 `sim/1D = 2.785`。一维谐振子的边缘分布不可能比它自己的
解析宽度宽 2.8 倍，所以在把那个数当物理之前，先查工具。

**我第一次查错了两次，两次都是设计错，不是代码错：**

1. 控制脚本把 `C.KB_KJ` 当成 kBT 用了。它是**每 K** 的 Boltzmann 常数
   （`KB_KJ = 0.008314462618  # kJ/(mol·K)`，torch_cgsim.py 208 行）。
   脚本打印的"精确值 0.004078"是错的，真值是 `sqrt(2.494/500) = 0.07063`。
2. 控制脚本固定的是**步数**而不是**模拟时间**，于是 `dt = 0.2 ps` 那一行比
   `dt = 0.002 ps` 那一行跑了 100 倍的时长。那个 dt 对比没有任何含义。

**改法：固定模拟时间，并且先测一个与任何势都无关的量。** 自由粒子（无力，只有 O 步）的速度是
纯 OU 过程，其平稳二阶矩必须是 `⟨v²⟩ = kBT/m = 2.2676e-4 (nm/ps)²`，与势无关：

| γ | dt (ps) | ⟨v²⟩/精确 | 100·2γdt |
|---|---|---|---|
| 0.1 | 0.002 | 0.0382 | 0.04 |
| **1.0** | **0.002** | **0.3985** | **0.4** |
| 0.1 | 0.02 | 0.3897 | 0.4 |
| 5.0 | 0.002 | 1.9942 | 2.0 |

四个点全部落在 `100·2γdt` 上（偏差 0.3–4.5%）。

**原因在 `batch_langevin_step` 的 O 步（1633、1643–1644 行）两处：**

```
c1 = exp(-γ·dt);  c2 = sqrt(1 - c1²)
sigma_base = sqrt(2·γ·KB_KJ/m)                    # 力项那一步是 /unit_conv 的，这里没有
noise = randn * sigma_base * sqrt(T) * sqrt(dt) * c2
```

该 O 步的平稳方差 = `sigma_base²·T·dt = 2γ(KB_KJ/m)·T·dt`；
而目标（积分器用的是 nm/ps 单位）是 `KB_KJ·T/(m·unit_conv)`，`unit_conv = 100`（1632 行）。

**所以 `⟨v²⟩ = 2γ·dt·100 · 目标`，即有效温度 `T_eff = 200·γ·dt·T`。**
噪声振幅既少了力项那一步的 `1/sqrt(unit_conv)`，又多了一个 `sqrt(2γ·dt)`。

**出厂参数 γ = 1.0 /ps、dt = 0.002 ps，所以 `T_eff = 0.4 T`。**
`isrnaclong.py:2024` 构造 `BatchedREMD2D` 时不传 `dt_ps`，也不存在 `friction` 参数
（`torch_cgsim.py:1897` 默认 0.002）；三处调用（1712、2288、2466）也只传 `dt_ps`。
**标称 300–1000 K 的 REMD，实际跑在 120–400 K。**

键合那组是独立佐证：σ/精确 = 0.188 / 0.322 / 0.555 / 0.599 / 0.915，
正好是 `sqrt(100·2γdt)` = 0.200 / 0.316 / 0.632 / 0.632 / 1.000。**它读的就是有效温度。**

**CPU 那份不受影响。** `openmm_gpu_refiner.py`（30 行）和 `rest2_remd_2d.py`（429 行）
都用 OpenMM 自己的 `LangevinMiddleIntegrator`。
所以这台机器（`torch.cuda.is_available()` = False，走 CPU 回落）上产出的历史结果没有被污染 ——
**受影响的是 GPU 路径，而 GPU 路径正是目标。**

**顺带一条：** REMD 的交换判据用 `beta = 1/(KB_KJ·T)`（1720 行），按**标称**温度算，
而实际分布是 0.4T。交换逻辑与动力学不自洽。

**已修。** O 步的噪声振幅改成 `sqrt(KB_KJ·T/(unit_conv·m))·c2` ——
把 `2·γ` 换成 `1/unit_conv`，并去掉 `sqrt(dt)`。回归测试 `tests/test_integrator_thermostat.py`：

| 测的量 | 精确值 | 修前 | 修后 |
|---|---|---|---|
| 单步噪声方差（四种 γ,dt） | `kBT/(m·unit_conv)·(1−c1²)` | ×(0.04 / 0.4 / 0.4 / 2.0) | 全部在 ±2% 内 |
| 自由粒子平稳 ⟨v²⟩（四种 γ,dt） | `kBT/m` | 0.0382 / 0.3985 / 0.3897 / 1.9942 | **0.9550 / 0.9962 / 0.9743 / 0.9971** |
| 单键热宽度 σ | `sqrt(kBT/k)` | — | 0.86–1.03（见下） |

`python -m pytest tests/` → **22 passed**（原 15 + 新 7）。

顺带：交换判据在 1720 行按标称温度算 `beta`，而那本来是**对的** —— 错的只是动力学。
修完两边自洽，1720 行不用动。

**但这个修里藏着一个前提，是紧接着才浮出来的：** 我把噪声配平成 `kBT/(unit_conv·m)` ——
那对 **11000 amu** 的珠子恰好是对的。见 §3ai。

**一条我写错的测试。** 单键那条第一次挂在 `mean r = 0.60733` 对 `r0 = 0.590`。
势只依赖 |r|，所以平衡密度带径向测度 `r²`，均值外移 `2kBT/(k·r0) = 0.01691 nm`，
实测位移 0.0173 —— 差 2%。**是测试错，不是积分器错。** 改成断言解析值反而更强：
现在它同时验测度。

**一项未闭合：键宽。** 我想用 dt 扫描分离「第二个 B 步复用旧力」残留的偏差，
**结果不可用，因为我把弛豫时间算错了**：用了 m = 110，而两个珠子各 110，**约化质量是 55**，
于是 `ω = sqrt(k/μ) = 0.301 /ps`、`τ = γ/ω² = 11 ps` —— 15 ps 只有 **1.4 τ**。
那一版扫描的数（0.709–1.028，非单调）读不出任何趋势。
40 ps 那组（3.6 τ）的 σ/精确在 0.861–0.996 之间跳，同样没有干净趋势。
**所以「O 步已精确，B 步的旧力还残留多少」只做了一半：速度那一半是精确的，位置那一半没测出来。**
干净的做法是从精确平稳分布出发测漂移方向，而不是从 δ 出发等弛豫；没有做。

### 3ai. 第二处：力踢小了 100 倍，时钟慢 10 倍（实测），以及顺带查出的第三处

**起因是 §3ag 的重跑。** 恒温器修好之后，IBI 第 0 轮的短飞检给出
`closest bead pair ever 0.0252 nm, pairs below the cutoff 43324` —— **链在塌**。
于是先不看统计，先看动力学。

**测质量，用周期。** 两个 110 amu 的珠子挂在 500 kJ/mol/nm² 的弹簧上，约化质量 55 amu，
这是纯经典力学，周期是算出来的：

`k = 500 kJ/mol/nm² = 0.83027 N/m`，`μ = 55 amu = 9.1330e-26 kg`，`ω = 3.0151e12 rad/s`
→ **T = 2.0839 ps**。

`scripts/integrator_mass_probe.py`（摩擦 0、无噪声，所以恒温器整个关掉）实测：

| | 半周期均值 | 周期 | 与解析比 |
|---|---|---|---|
| 修前 | 10.4194 ps | **20.8389 ps** | **10.000** |
| 修后 | 1.0419 ps | **2.0839 ps** | **1.000** |

**所以 `unit_conv = 100` 把两次力踢都缩小了 100 倍，有效质量是 `mass_amu` 的 100 倍。**
那个 100 属于以 kJ/mol/Å² 为单位的**力常数**（1 kJ/mol/Å² = 100 kJ/mol/nm²），不属于力。
在 nm/ps/amu/kJ/mol 这组单位里换算系数**精确等于 1**，而且 `kB = 0.0083145 kJ/(mol·K)`
在 amu·nm²/(ps²·K) 里是同一个数。

**为什么一直没有东西发现它：平稳分布与质量无关。** 能量、力、梯度、所有平衡量全部是对的，
错的只有时钟 —— 而没有任何测试看时间。

**更糟的是我上一轮把这个错锁进去了。** §3ah 里我把噪声配成 `kBT/(unit_conv·m)`，
那对 11000 amu 的珠子是**精确正确**的。所以系综看起来对，而每一个时间尺度都长了 10 倍。
`dt_ps = 0.002` 一直是 **0.02 ps 的真实动力学**。

**而这和文档自己对不上。** §3ae 写「100 Da 珠子的热速度 273 m/s、每步位移约 5e-4 nm」，
那是按物理质量算的；代码给的是 **15.06 m/s 和 3.0e-5 nm**，差 17 倍。

**已修**：两次力踢去掉 `/ unit_conv`，噪声改成 `sqrt(KB_KJ/mass)`。
测试 `test_force_kick_uses_the_stated_mass_not_a_hundredth_of_it` 用周期锁住它。
`python -m pytest tests/` → **23 passed**。

**一个项目级的后果：** 同一组 `rest2_nsteps` 现在覆盖的真实时间是原来的 10 倍。
原来那批 GPU 结果是拿 10 倍慢的时钟调出来的，步数可能需要重新看。

---

**第三处，是验第二处的时候顺带查出来的，没有修。**

摩擦 0、无噪声时这个格式是纯确定性的，而它**不是辛的**。把一步写成映射：

```
v_{n+1} = v_n + dt·a(x_n)
x_{n+1} = x_n + dt·v_n + (dt²/2)·a(x_n)
```

Jacobian 行列式 `= 1 − (dt²/2)·a' = 1 + (dt·ω)²/2`（谐振子）。**相空间体积每步长这么多，
能量跟着长。**

实测对得上：`dt = 0.002`、`ω = 3.015 /ps`、40000 步，`(1 + 1.8e-5)^40000 = e^0.72 = 2.05`；
而振幅区间从 0.010 nm 走到 0.0072–0.0143 nm，**能量 ×2.04**。

**病根就是第二个 B 步复用旧力** —— 代码自己的注释写着「using the same forces; a simplified
version」。真正的 BAOAB 那里要放 `F(x_{n+1})`，而那样才是辛的。

**为什么平衡量还是对的：** 出厂参数下拖拽是 `2γ·dt = 4e-3`/步，泵入是 `1.8e-5`/步，
**差 220 倍**，所以平稳态只被扰动约 0.45%。这就是所有平衡测试都能过的原因。
**它只在大 dt、或者任何依赖动力学而非系综的场合才咬人。**

修它需要积分器自己再算一次力（调用点有三处），成本翻倍，是结构性改动。**没有做。**

### 3aj. 先最小化再跑：此前「塌陷」读数的一半是未松弛的起点（实测）

这一节从一句错话开始。§3ah/§3ai 之后我一直在报「GPU 路径的恒温器跑到 979 K」。
**那个数字是未松弛起点造成的，不是恒温器。撤回。**

本会话所有塌陷测量都从**原始沉积坐标、v = 0** 出发。第一个采样点的动能温度是 29–72 K，
然后升上去 —— 一个非平衡起点会做的正是这件事：势能转成动能。
管线自己不这样：Level 1.5 / 2.5b 调 `physical_relaxation`，REMD 驱动在主循环前先跑 500 步。

用场自己的力做单调阻尼最速下降（能量上升时步长减半，下降时 ×1.2），1L2X（27 残基 / 81 beads）：

| | E (kJ/mol) | max\|F\| (kJ/mol/nm) | 距起点 RMSD |
| :-- | --: | --: | --: |
| 起点 | 1196.56 | 1226.71 | — |
| 最小化后 | −1665.70（2365 次迭代，6 次重启） | 345.20 | 0.4024 nm |

力地板 `sqrt(k·kBT)` 对 `K_INTRA_PC` 是 236.3，所以 345.20 已经是这套判据能到的量级。

从最小值出发再跑 15 ps：**后 1/4 段 302.3 K**（目标 300 K，1.01×）。**恒温器是对的。**
979 K / 606 K / 1000 K 那三个数是「起点未松弛」加「时间不够」叠加出来的。
但同一次跑的势能在 15 ps 里还在下降 **1984.5 kJ/mol**（末端 −3501 → −3648 未平），
所以那个 302.3 K 是「行为正确」，**不是平衡态数据**。

### 3ak. 四个没有项的骨架对：枚举出来的，不是手写的（实测 + 已修）

3-bead 核苷酸是 P–C4'–N9/N1，只有两根键（`K_INTRA_PC`、`K_INTRA_CN`），
而排除体积在**两条路径**上都跳过 `|i-j| <= 2`。把**每个 bead 对**按序列间隔 1、2、3 全枚举
（九类，从 bead 编号推出，不从记忆里列），落出四对既没有键项、也没有排除体积：

| 对 | \|i-j\| | 数据库 min | mean | sd | k = kBT/sd² | 力地板 |
| :-- | --: | --: | --: | --: | --: | --: |
| P(i)–N9/N1(i) | 2 | 0.3801 | 0.5370 | 0.0374 | 1785.9 | 66.7 |
| C4'(i)–P(i+1) | 2 | 0.2810 | 0.3800 | 0.0161 | 9574.4 | 154.5 |
| N9/N1(i)–P(i+1) | 1 | 0.4731 | 0.5457 | 0.0213 | 5477.7 | 116.9 |
| N9/N1(i)–C4'(i+1) | 2 | 0.3799 | 0.6303 | 0.0807 | 383.0 | 30.9 |

**第一版审计脚本手写了六行类别表，因此只找到三对，漏了第四对。**
手写的表找不到作者忘掉的那一类 —— 第四对是回归测试（`tests/test_backbone_13_terms.py`
里的互补检查）抓到的，不是我自己看出来的。枚举现在在 `scripts/audit_intra_residue_pairs.py`。

修前实测（1L2X，27 残基 / 81 beads）：
- 最小化把 P(15)···N9(15) 压到 **0.1511 nm**，这一对的数据库最小值是 0.3801；
- 3 ps Langevin 的 900 个采样帧里 **811 帧**的最深接触落在这四对上，最深 **0.0058 nm**，
  即珠子互相穿过；墙负责的那些帧最深只到 0.0863 nm；
- **墙本身没有故障**：单元测在 0.0863 nm 给 **608203 kJ/mol/nm**，且 cell list 确实包含该对。

修后（四个谐振项，判据与其它键合坐标完全一致：k = kBT/sd²、r0 = 数据库均值）：

| | 修前 | 修后 |
| :-- | --: | --: |
| 最小值处最近对 | 0.1511 nm | 0.3330 nm |
| 平衡后动力学最深 | 0.0058 nm | 0.2570 nm |
| < 0.30 nm 的采样帧 | 900 / 900 | 52 / 1126 |

**两个跨骨架连接的对只作用于 L−1 个内部连接，不环绕闭合处。**
环绕试过并撤回：常数取自**线性**数据库（没有闭合连接），而管线从伸展链起步，
环绕的谐振项会给 `K_BSJ` 正要驱动的那个压缩加上约 1e5 kJ/mol 的势垒
（在 `tests/test_force_gradcheck.py` 那条 4.72 nm 直线链上实测 +1.49e5 kJ/mol）。
闭合处的三对 bead 间隔是 3L−3、3L−2、3L−1，都 ≥ 3，所以墙管得到它们。
残留代价写明：两端合上之后 C4'(L−1)–P(0) 的数据库均值 0.3800 **低于** `CLASH_SIGMA` = 0.3975，
墙在往外压那一对。

**顺带查出一个「写而无读」**：`cg_energy_3bead` 算出 `e_intra` 却没放进返回表达式
（812–815 行的求和里没有它），所以那条路径报的能量里既没有 P–C4'、C4'–N9/N1，也没有新加的四项。
一直没被发现，是因为唯一调用它的测试是**两次相减**（`test_clash_single_potential.py`），
缺的项在两边都缺，就抵消掉了。

### 3al. 力帽不是热源（实测）

在最小值上分别开关力帽：cap 5000 → **450.9 K**；cap none → **458.8 K**。**不是帽子。**

同期我把三项加到「没有它们时求出的」极小点上，得到 1528 K 并一度当作「补项有害」——
那是测试错了：那个几何对新项有 **12987 kJ/mol** 应变，等于往系统里灌势能。
**加约束不等于加谐振项，最小值必须带项重算。**

### 3am. 排除体积：单一 σ 的代价量出来了，不值得拆（实测 + 否决）

两个不同的集合都叫「非键合对」，只有一个是这个项真正作用的：

| 集合 | 定义 | 对数 | 最近 (nm) | 落在 0.3975 内 |
| :-- | :-- | --: | --: | --: |
| A | 残基间隔 ≥ 3（P–P 标定用的那个） | 1,923,588 | 0.3333 | 66（0.00343%）|
| B | **bead 间隔 ≥ 3（力场实际作用的那一个）** | 2,022,024 | **0.3092** | **167（0.00826%）** |

按 bead 类型拆开，六种类型对各自的最近距离：
P–P 0.3975 / P–C4' 0.3661 / P–N9 0.3936 / C4'–C4' 0.3333 / C4'–N9 0.3673 / N9–N9 0.3411。

**否决按类型拆 σ，三条理由都是测出来的：**

1. 它消掉的是 2,022,024 对里的 **167 对，0.0083%**；
2. 它会把 C4'–C4'（0.3333）和 N9–N9（0.3411）**变弱**，而不是变强 —— 现在的单一 σ
   对这两种类型是**过紧**的；
3. 判据给不出按类型的 k：σ 定义成该类最小值，所以下箱对**六种类型全部为空**，反演没有输入。
   （顺带说明：现有的 `K_CLASH = 20000` 是从**一个**观测反演出来的。）

**墙没有被穿透。** 我此前报「一个 |i-j|≥3 的对到 0.3022 nm，低于数据库最小值 0.3333」——
那是**标签错误**：`check_field_after_fix.py` 只拿全局最近对去查 `SHORT`，没有 `BONDED` 兜底，
于是 0.3022 的那个 `P(i)–C4'(i)` **键内**对被叫成了「wall-applied」。
屏蔽到 |i-j| ≥ 3 重测，15 ps 内最深 **0.3411 nm**，**高于** 0.3333。
在那个构型上做力的分解（投影到两珠连线上，正为推开）：
墙 +1232.41，pair guide −287.63，bb bond −99.01，intra P–N9 −90.50，link N9–P −73.31，
其余 −1.05 到 +28.70，**合计 +714.17**。**是力平衡，不是被推穿。**

### 3an. K_PAIR 终于有上界：上界一直在，只是要一个会翻转的诱饵（实测）

`tests/test_pair_clash_bsj_criterion.py` 自己写着：「nothing measured selects 600 over 1500 …
choosing inside the bracket needs a fold or ranking run」。这次跑了那个 run。

因为 WC 项是 `0.5·K_PAIR·λ·w_k·(d_k − PAIR_NN)²`，整个 K_PAIR 依赖是**解析线性**的：

    ΔE(K_PAIR) = K_PAIR·A + ΔE_guide

A 从几何直接量，ΔE_guide 是 K_PAIR 置零后的差（剩下 pair guide / bpp / BSJ guide / BSJ contact）。
所以扫描是精确的，不是网格重跑。正 ΔE 表示力场把**错误**配准评得更低。

| 诱饵 | A 的符号 | 结果 |
| :-- | :-- | :-- |
| 随机重配对（容易） | 40/40 为正 | **单调**：K = 0 时 30/40，K = 100 时 39/40，**K ≥ 205 起 40/40**，之后只增 |
| 配准位移 1 位 | 0/35 为正（全负） | **会翻转**：35/35 到 **K = 2173.9**，2500 → 32/35，4000 → 21/35，8000 → 10/35 |
| 配准位移 2 位 | 22/29 为正 | 29/29 到 2500，4000 → 28/29 |

配准位移诱饵会翻转，是因为**它自己的 N–N 均值 1.1426 nm 比正确的 0.9402 更接近** `PAIR_NN` = 1.0
—— 所以硬化弹簧最终会让错误配准变得「更合目标」。容易的诱饵做不到这件事。

**括号从 [204.8, 47035] 收到 [204.8, 2173.9]。**

但要说清楚它**没有**做到的事：它**不区分 600 和 1500**，两个都是 35/35（1500 的余量只有 1.45×）。
「选 600 还是 1500」仍然没有测量能回答，能回答的只是「不能超过约 2200」。
这一条写进了测试，不是留在散文里。

### 3ao. 我在这一天里犯的归因错误（四个，全部撤回）

记下来是因为它们的**形状相同**：拿一个模型的约定去套另一个模型。

1. 「979 K 是恒温器坏了」—— 是未松弛起点，见 §3aj。
2. 「补三项有害（1528 K）」—— 我把项加到了未重算的极小点上，见 §3al。
3. 「0.3022 nm 说明墙被打穿」—— 默认标签吸收了 `BONDED` 类别，见 §3am。
4. 「`cg_energy` 的 `d_bb = pos[:,1:] − pos[:,:-1]` 是缺陷」—— 它是 **1 bead/残基**的模型
   （测试名 `test_the_one_bead_path_uses_the_same_potential`，几何 `(1, L, 3)`），
   相邻 bead 就是骨架 P–P，`|i-j| ≤ 2` 就是标准的 1-2 / 1-3 排除。**它没有缺陷。撤回。**

1 和 3 是同一个病：一张我没查全的类别表/标签表，替我「补上」了一个我没意识到的类别。
修法不是更小心，是**枚举和屏蔽** —— 能从编号推的表就不要手写，能屏蔽的就不要用默认分支。

### 3ap. 「算出来就忘了」的静态扫描：`src/` 里除已修的那一处没有第二例

`cg_energy_3bead` 那个 `e_intra` 的形态值得做成一个检查。规则只有一句：

> **一个候选量只要在任何地方被读到、而那处不是它自己赋值的右边，就算已用。**

比「必须到达 return」宽 —— 只喂了诊断字符串或 numpy 槽数组的值也算在用，
而 `e_intra` 修之前的**唯一**读取正好在它自己的 RHS 里（`e_intra = e_intra + (...)`），
所以照样会被抓到。

**前两版都错在这里，而且都是拿 `cg_energy_3bead` 当已知答案才发现的：**

- 第一版把数据流闭包走反了，于是把 `cg_energy_3bead` 收进 `energy` 再返回的**每一个**项都报出来
  （包括 `e_intra` —— 对的，但理由是错的，而且被淹在二十多条里）；
- 第二版要求读取必须到达 `Return` 或 `total_* +=`，于是误报 `e_slot_np`
  （写进 `energies[slots] = ...`）、`e_cg_min`（被一个比值读）、`e_r`（被 `.backward()` 读）。

**扫描器本身也需要一个已知答案才能验证。** `tests/test_no_unreturned_energies.py` 里放了一个
故意写坏的小函数钉住这件事，另加一条「结果为空则失败」的断言。

现状：`src/` 54 个文件、**2 个候选**，都是 `physical_relaxation.py:376` 的 `e_stack`
—— 那是**已记录的有意省略**（注释：「e_stack 构造了但不返回 …… 让这个省略保持可见，
否则把 K_STACK 打开会看起来像恢复了约束却什么都不改」）。
**除已修的那一处，`src/` 里没有第二例。**

顺带一个小陷阱，记下来因为它骗过了一轮：这个脚本原来在**导入时**读 `sys.argv[1]`，
pytest 下那里是测试文件路径，于是 root 变成一个文件、`rglob` 什么也不扫、`found` 为空 ——
而测试的前两条断言在空集上**照样通过**。argv 现在只在 `main()` 里读。
**「检查通过了」和「检查真的跑了」是两件事。**

### 3aq. `pair_w`：那个「从没测过」的通道测了 —— 它工作，而且不能用互补性改进

`isrnaclong.py:780` 自己写着方法一致性权重（1.0 hard / 1.0·0.8·0.6·bpp_mid soft）
**从没被测过判别力**，那需要 MFE/PF/DivideFold。本机有 ViennaRNA **2.7.2**，所以这次真跑了。

把权重作为第四个 score 接进现成的 harness（`scripts/measure_pair_weight_quality.py`，
score 名 `method_agree`）：用**管线自己的**设置（`md.circ = 1`）和它自己的层界（P>0.9 high，0.5<P≤0.9 mid），
按它的顺序组装（hard → MFE 0.8 → PF mid 只在两者都没占时，因为它的 `hard_set` 是边追加边看的）。
DivideFold 本机没有，所以 0.6 那一层为空 —— 那正是管线在子进程失败时的状态，写明，不默认。

| score | N1 AUC | N1 打乱 | N2 AUC | N2 打乱 |
| :-- | --: | --: | --: | --: |
| `rcm_confidence` | 0.5044 | 0.5029 | 0.5519 | 0.5006 |
| **`method_agree`** | **0.7080** | **0.4986** | **0.7075** | **0.5005** |
| `wc_identity` | 1.0000（构造） | — | 0.8333 | 0.5034 |
| `geom_in_band` | 0.5000（构造） | — | 0.9833 | **0.9833** |

配对 cluster bootstrap（同链重采样，1000 次的那套）：

- `method_agree − rcm_confidence` = **+0.2036 [+0.1583, +0.2425] p=0.000**（N1）、
  **+0.1556 [+0.1129, +0.1973] p=0.000**（N2）
- 序列特异成分：`method_agree` **+0.2094 / +0.2070**，`rcm_confidence` +0.0015 / +0.0513

**结论一：这个通道工作。** 关掉 RCM 重加权是对的，而现在有数字了 —— 不再是「看起来更差」。

**结论二：它不能用互补性改进。** `ma_x_wc`（`method_agree × wc_identity`）与 `method_agree`
在**两个集合上完全相同**（`+0.0000 [+0.0000, +0.0000]`）—— 因为**凡是拿到权重的对，本来就已经是 WC 互补的**。
「把碱基互补乘进 `pair_w`」是恒等变换。这一条记下来，免得再被提一次。

**结论三（这是数据的限制，不是方法的问题）：这个数据库上测不了「化学和几何之外还有没有信息」。**
本该做这件事的第三个负样本集 —— 间隔匹配 **且** 同带 **且** 互补，把化学和几何**都**按住 ——
**按构造为空**：`_chain_residues` 接受一个对的条件正好是「WC 兼容 ∧ 间隔 ≥3 ∧ C1' 在 9.0–11.5 Å 带内」，
所以**标签本身就是那个合取式**，任何分数都无法与它对照。40 条链上 N3 的池子是 0，不是稀薄。
要测这一层只能换数据（比如把真配对按实验可及性扰动，或者用非天然但合理的错配）。

**结论四：N2 不是公平的场地。** `geom_in_band` 单项在 N2 上就是 **0.9833**，而它打乱字母后**还是 0.9833**
—— 序列成分为零。所以「N2 上 `wc_identity` 0.8333 赢过 `method_agree` 0.7075」是在一个**几何主导**的
集合上做的比较，两者都不是在回答「配对项有没有用」。干净的是 N1（几何按住、化学移除），那里 **0.7080**。

**所以：`pair_w` 不改。** 这是有测量支持的「不动」，不是没查。

## 5. 下一步

**已定的方向（§3l）：路 1 先做，路 2 是真正的答案。**

1. ~~**路 1（最小改动）**~~ —— **已落地，见 §3m**。`STACK_R0` = 1.125 nm、
   `DIH_PPPP` = `acos(0.975)`、`_R0_STACK` 快照已删。留出集 native rank 6.75 → 1.19。
   `ANGLE_PPP` 未动。回归测试在 `tests/test_ff_bonded_targets.py`。
2. ~~**路 2 第一步（DBI）**~~ —— **已做，见 §3o**。六条键合坐标的 `-kBT ln P` 表建好，
   自动微分取力（对中心差分 6.0e-09），留出集漏斗检验 **rank 1.00 / margin 0.0**，
   对调和项的 1.22 / −88.9。**库未改**，玻尔兹曼势只在 `scripts/boltzmann_bonded.py` 里。
2b. ~~**定标**~~ —— **已做，见 §3p**。结论：漏斗对标度不敏感（1000 倍范围内 rank 都贴
   1.000），形状才是差别所在；"曲率配平"不可用（拟合曲率自身与 `kBT/σ²` 差最多一千倍）。
   **保持 DBI 字面标度，标度交给 IBI。** 这一条把 IBI 从可选变成必需。
2d. ~~**两条库路径差 18%**~~ —— **查清了，见 §3q**：`cg_forces_explicit_batched` 是**死代码**，
   `src/` 里没有调用者；它是另一个力场（少了 bpp / pair guide / BSJ contact，GB 系数差 4 倍）。
   **不构成 IBI 的障碍。** 但顺带查出活路径有**两个力是错的**（angle 192.7%、dihedral 183.6%），
   已修，十三项现在全部自洽到 5.6e-07。
2e. **障碍（§3q/§3r）：力帽在 26% 的珠子上生效，所以 `F = -dE/dx` 按构造成立不了。**
   病根是 **angle 与 dihedral 的刚度**（p95 分别 755、3218 kJ/mol/nm；三个局部项 max 才 158）。
   **§3q 里我给的机制（φ→0 奇异）是错的**，改成约束 φ 也修不掉（两者共用 `dφ/dx`）。
   **§3r 已证明整体调软对漏斗是恒等的（3000 倍范围 rank 小数点后三位不变）** ——
   所以杠杆是标度，而且零代价。**在标度解决之前，IBI 迭代的是被帽子截断的力，没有意义。**
2ae. **GB 台阶在真实步长下量了（§3ae）**：`dt_ps = 0.002`（2 fs），热速度 273 m/s，
   **每步位移约 5e-4 nm**，那里台阶是 **0.012 – 0.31 kBT**。小，但对 REMD 判据不是零。
   **未加切换函数。** 而我在这条里犯过一个符号错误（残余写成 `−2F·u`，量到的是 `4|F·u|`），
   症状是残余随 δ 线性增长、且恰为光滑列的 2 倍。已修。
2ab. **clash 的三个缺陷修了（§3ad）**：邻居表按副本 0 建、只枚举同格、掩码按副本 0 ——
   三个都是静默的，**64 个副本里有 63 个没有排除体积**。修后验证通过。
   而它在天然结构上不响是因为最近的一对珠子是 0.309 nm、阈值 0.300 —— **对的，只是没活干**，
   但离阈值只有 0.009 nm，是一道刀刃。
2ac. **gradcheck 解决了（§3ad），而答案是「这两个检查本来就做不到」**：
   统计量（逐分量比值）分母穿过零、自己会炸；**而整条路径的能量本身不连续**
   （GB/SA 的配对表按硬 cell 过滤每次重算，一对进出跳 0.074 kJ/mol，正是 `2·exp(−1/0.304)`）。
   测试改成测十三个解析项 + 力帽 + **把不连续性作为实测事实写进测试**。**15 passed。**
   新增 `force_cap=None` —— 那个帽子按构造破坏 `F = −dE/dx`，必须能关掉才能分开测。
2ad. **统计势不在力场里**（grep 实测 `src/` 零引用）。**但它改了这个力场** ——
   四个被重定标的常数全部来自对实验结构分布的统计（众数、`kBT/σ²`、`0.6·kBT/σ_NN`）。
   **它定的是「目标在哪、该多硬」，不是「再叠一个能量项」。** 没做的那一半是 IBI。
2y. **stacking 关掉了（§3ac）。** 恒等式 `|P(i)−P(i+2)|² = |b_i|²+|b_{i+1}|²−2|b_i||b_{i+1}|cos_a`
   **精确成立（R² = 1.000000，残差 6.7e-16）**，而两个键和 `cos_a` 各有约束项 —— 它是第三个弹簧压在
   导出量上。消融后 rank/gap 不动而超帽 4.02% → 0.58%。**`K_STACK` 500 → 0.**
   而且模型根本没有 base 平面，**表达不了 stacking**。
2z. **顺带：`kBT/σ²` 对键项不成立** —— 调硬反而把超帽从 0.58% 推到 1.61%。键项保持 500，测试锁住。
2aa. **整场 53.77% → 3.08%（ones）/ 44.24% → 3.02%（RCM），而 p95 从 200.00 掉到 149.86 / 141.27**
   —— **帽子不再是力律**，这是前面每一节都没有达到的状态。
2w. **环状测试结构：试了，失败了（§3ab）。** 循环平移确实让闭环项归零（5249.57 → 58.42），
   **但它凭空造了一根跨原链两端的键**（原两端平均相隔 5.012 nm），于是 `bb bond` 从 30.4 涨到 2513.7。
   **这和我在加载器里修过的间隙 bug 是同一类错误。线性结构不是环，改编号不能把它变成环。**
2x. **退回到上界**：线性结构上整场超帽 **6.14%**，其中 BSJ 三项自身贡献 1.24 个百分点，
   所以去掉假象后真实值在 **5–6.14%** 之间（精确值给不了 —— 帽子施加在总力向量上）。
   **`bsj closure` 那 90.7% 的能量占比是工况产物，但它对超帽只贡献 1.18 个点 ——
   所以 §3aa 那个 6.20% 的改善不是靠它撑的。线性结构上真正顶帽的是 stacking（1.81%）。**
2t. **bpp 定标做完了（§3aa）**：判据 `K_BPP = 0.6*kBT/σ_NN = 13.4`，网格证实
   （100→300 之间一道陡坎；折叠信号 10–600 平 16/16，3.0 崩到 8/16）。**K_BPP 600 → 13.4 已落地。**
   **整场顶帽 53.77% → 6.20%（ones）/ 44.24% → 6.14%（RCM）**，bpp 自己的超帽 22.31% → 0.00%。
2u. **一个新发现的系统性缺口**：我所有的「整场」数字都含 `bsj closure`，而它是闭环项，
   **测试用的却是线性结构** —— 那个 90.7% 占比是工况产物。要干净地量整场需要**环状测试结构**，现在没有。
2v. **剩下顶帽的是 stacking（1.81%）** —— 下一个候选，而它的合理性从 §3q 起就被质疑。
   `tests/test_force_gradcheck.py` 仍然红，但 max_rel 一路在降（11.42 → 7.17 → 5.85 → 3.09），
   **「帽子」那条解释仍未确认**。
2r. **整场前后对比补上了（§3z）**：53.77% → **28.44%**（ones），44.24% → **19.16%**（真实权重）——
   大致减半，整场确实变好。**但 p95 仍是 200.00，28% 的珠子精确顶在帽上，帽子还是力律**；
   子集那个 3.25% understate 了约九倍。
2s. **现在顶着帽子的是 bpp**（贡献 28.44 里的 22.31）。而它**必然**顶帽，不是调参问题：
   `E = -K_BPP*w*softplus((1-r)/0.3)` 在目标距离 r=1.0 处 sigmoid(0)=0.5，
   所以每对恒施加 `600/0.3*0.5 = 1000` kJ/mol/nm —— **帽子的五倍，与几何无关**。
   而且 softplus 是单边的，没有回复分支。要落到帽下需要 **K_BPP ≤ 120**（w=1 时），多对共珠还要更低。
   **下一步：给 bpp 一个从数据来的判据 + 一张网格 + 一次前后对比**，和上次同样的做法。
2q. **改的是 GPU 版，而这台机器不走它（§3y）。** 力场有两份并行实现，六个常数不一致；
   `torch.cuda.is_available()` 实测 **False**，所以 Level 4 走 CPU 回落（`openmm_gpu_refiner`），
   Level 1.5 也走它。**三个层级里两个不吃我的改动。**
   我上一轮说「管线走的是活路径」时读的是 GPU 那一支、没读它的条件 —— 这是我的疏忽。
   **`openmm_gpu_refiner.py` 的 K_ANGLE 600 / K_DIHEDRAL 800 按 `kBT/σ²` 判据也错 21 倍和 111 倍，
   但不能照抄**（二面角目标不同、堆叠项在不同原子对上）。
2o. **力场现状有了实测（§3x），并更正一条**：重定标之后 **bpp 成了整个力场**
   （占净能量 100–118%，单项超帽 22.31%；真实权重下 101.1% / 13.02%），
   而 angle+dihedral+stacking 加起来只剩 3%。
   **我报的「超帽 26.26% → 3.25%」是六个键合项上的数，不含 bpp** ——
   整场的前后对比我没做过，是我把子集的改善说成了力场的改善。
   **下一步应该是补这个对比**，而不是继续往下走。
2p. **两处未解释**：`bsj closure` 有全场最大的单点力 5249.57（但它是闭环项，对线性测试结构本就不该存在）；
   `clash` 一次都没触发，原因未排除。
2m. **pair_w 那条门关了（§3w）**：管线用 RCM confidence 替换全部权重，实测中位 0.25、约 40% 近零，
   所以 `pair_w = ones` 是**上界**不是等价工况。真实权重下折叠信号**活着但弱了**（合计 22/24，bpp 量级掉三倍）。
   顺手修了 `cg_force_terms.py` 里 bpp 漏乘权重的第三次漂移。
2n. **冒烟测试过了（§3w）**：`BatchedREMD2D.run`（管线用的入口）带真实形状权重跑通，输出有限。
   **但真·端到端还没跑过**，所以「离接入还有多远」仍未测量。
2k. **近天然诱饵验过了（§3v），结论要打折**：配准位移下 `wc pair` 掉到 **42/111**（比抛硬币差），
   **全部判别力来自 bpp 一项（111/111）**，而 bpp 本质是距离判据。更糟的是**我所有测试都传
   `pair_w = ones`，而这是力场唯一的序列依赖** —— 相当于把化学关掉了。要问配对识别，
   必须带真实的 `pair_w` 再测一次。
2l. **常数落地（§3v）**：`K_ANGLE` 600 → **28.1**、`K_DIH` 500 → **7.2**，
   `_K_*` 同步，测试锁住。超帽 **26.26% → 3.25%**，p95 **3506.53 → 166.65**，
   angle 项不再超帽。**剩下超帽的是 stacking（46）和 dihedral（15）** —— 下一个候选是 stacking。
   但 **`tests/test_force_gradcheck.py` 仍然红**，而且绝对误差腰斩、相对误差没动，
   所以「帽子导致 `F ≠ -dE/dx`」这条解释**仍未确认**。
2j. **非键合项验过了（§3u）：能承重，而且配对项是唯一的承重者。**
   长度自配对诱饵下 wc pair **24/24**、bpp **23/24**、总数 23/24；十一条几何项全部精确为 0.0
   （负对照）。**能量账本同时证实 §3t**：dihedral 单项最大（6,946），angle 1,908，而三个局部项
   加起来 34 —— 占着能量预算的两项做的是零折叠功。
   **但诱饵太粗**（随机重配对把伙伴拉得很远，配对能量必然爆），要问"能不能挑出似是而非的错折叠"
   需要**近天然诱饵**（配准位移）。这是下一个测试。
2i. **消融做完了（§3t），结论变了。** angle 和 dihedral 对漏斗不做有用功（两个都关掉后
   rank 1.000），却扛着全部的力（超帽 27.1% → 1.9%）。**建议 `K_ANGLE` 600 → 28.1、
   `K_DIH` 500 → 7.2** —— 这正好是 `kBT/σ²` 给出的比例，两条独立的线合上了。
   **下一步不是 IBI，是先看非键合项能不能承重**（折叠现在全压在 pair / bpp / stacking 上，
   而那三项一次都没验过）。
2g. **重定标已做（§3s）**：表在清洗数据上重拟合了，三个局部项的 k 定了（1129.2 /
   20752.7 / 36399.2），混合势建好了。**方差验收失败，而失败在测试设计** ——
   只有键合项的链会解开，而参考边缘分布以折叠为条件。
   **更有用的一条：匹配方差和保持几何冲突**（dihedral 按 kBT/σ² 只有 3.6 kBT 阱深，
   兜不住构象）。**根源是池化的数据库 σ 混了不同构象态，不是任何单态的宽度。**
   **所以 IBI 是唯一出路**，`scripts/sample_bonded_chain.py` 是外循环缺的那一半。
2h. **还剩两件事**（§3o/§3p）：
   - **换更硬的诱饵**（别的折叠/别的序列的天然结构，而不是高斯扰动），否则"1.00"不能当折叠判别力。
   - **定标问题**：`-kBT ln P` 深度几 kBT，调和项 k≈500 给几百 kJ/mol，差一个数量级。
     换进整场后非键合项仍主导。要不要给玻尔兹曼项定标，这次结果没有回答，反而让它更要紧。
   - **IBI**：DBI 的已知弱点是不管自由度之间的关联。要判断是否需要 IBI，
     得先测**模拟出来的分布**能否复现参考分布 —— 那是下一步该跑的东西。
3. **不要整体换用现成模型** —— 珠子定义不匹配（NAST 单珠在 C3'、oxRNA 刚体多作用位点、
   SimRNA/RNAJP/IsRNA2 五珠、Martini 3 4–7 珠；三珠 P/C4'/N 的只有 cgRNASP，而它是打分函数）。
   方法可以照搬，参数不能。

**仍未解决，独立于上面：**
- **bpp 是现在最大的一项**（263.96 kJ/mol/nm，前因子 `K_BPP/0.3 = 2000`）。下一个要定的是它。
- **两条库路径的能量差 18%**（53,711.73 vs 45,441.82 kJ/mol），且生产路径 `cg_forces_explicit_batched`
  没有力帽。独立于上面的常数。
- 常数改对之后要**重看那个 200 kJ/mol/nm 的帽子** —— 原始力降下来，它或许就不用再当力律。
- `_dihedral_f` 是注释自承的**近似梯度**，还用 0.25 的任意系数分力（620–633 行）——
  即使目标值改对了，这一项也还是近似。
- `STACK_R0` 改对之后在天然几何附近几乎不起作用（留出集 107.34 → 24.15），
  要想清楚 P(i)-P(i+2) 距离该不该用来代理 stacking。
- §3k 的弱点仍在：诱饵是高斯扰动珠子（物理上不成立），只做了一次 16/16 二分，没有交叉验证。
- **平衡时间不够（§3aj / §3ak）**：15 ps 之后势能仍在下降 1984.5 kJ/mol（末端 −3501 → −3648 未平），
  最小化残差 345.20 对力地板 236.3 卡在 1.46×。
  **所以「后 1/4 段 302.3 K」是行为正确，不是平衡态数据**；要报任何热力学量必须先跑到平台。
- **K_PAIR 的内部选择仍无测量（§3an）**：括号 [204.8, 2173.9]，而 600 和 1500 都在里面、
  都拿 35/35。要分开它们需要一个**更细**的诱饵，或者一个不是排序的判据（稳定性、力预算）。
- **按类型的 σ 已否决（§3am）**，三条理由都记在那儿。改这一条之前先重读那个 0.0083%。
- **`_ClashNeighborList` 是 1-bead 路径的潜在缺陷（未修，无生产调用者）**：
  模块级单例、只按 `pos_nm[0]` 建表、`rebuild_freq = 10`。
  这正是 `GPUCellList` 已修掉的「缺陷 1」，但在 `cg_energy` 这条路径上还在。
- ~~**`cg_energy_3bead` 的 `e_intra` 已补回（§3ak）**~~ —— **静态扫描做了（§3ap）**：
  `src/` 54 个文件，2 个候选，都是已记录的有意省略。**除已修的那一处没有第二例。**
  扫的规则是「只要在某处被读到、且那处不是它自己赋值的右边，就算已用」，
  并且 `tests/test_no_unreturned_energies.py` 用一个故意写坏的小函数钉住扫描器**能失败**。
- ~~**`pair_w` 是场里唯一的序列通道，而它几乎不工作**~~ —— **测了（§3aq），它工作。**
  `method_agree` 在 N1 上 AUC **0.7080 [0.6653, 0.7401]**，打乱后 0.4986，
  序列特异成分 **+0.2094**；`rcm_confidence` 只有 +0.0015。**所以 `pair_w` 不改。**
  但也要记着它测不了的那一层：**「化学和几何之外还有没有信息」在这个数据库上按构造无法测**
  （N3 的池子为空，因为标签本身就是那个合取式）。要回答它只能换数据。
- **`pair_w` 剩下的唯一可测缺口**：本机没有 DivideFold，所以 0.6 那一层在测量里为空。
  管线在子进程成功时会有这一层，而它**从未被单独测过**（§3aq 里它没有成员，所以那个数字不覆盖它）。